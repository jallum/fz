# Expected: main() : {:empty, int, :error}

fn pick({:ok, []}), do: :empty
fn pick({:ok, [h | _t]}), do: h
fn pick(:error), do: :error
fn pick(_), do: :unreachable

fn main() do
  {pick({:ok, []}), pick({:ok, [1]}), pick(:error)}
end
