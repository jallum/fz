# Pattern-dispatch binding flow. The cons leaf returns the bound head, proving
# matcher-produced bindings carry the input list element type into the leaf.
#
# Expected: main() : {:empty, int}

fn pick([]), do: :empty
fn pick([h | _t]), do: h

fn main() do
  {pick([]), pick([1])}
end
