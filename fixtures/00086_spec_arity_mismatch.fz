#---
# purpose: an @spec whose arity does not match its function's arity is a parse-time error.
# expect: diagnostic
# diagnostic.code: parse/spec-arity-mismatch
#---
defmodule M do
  @spec add1(integer, integer) :: integer
  def add1(n), do: n + 1
end

def main(), do: dbg(M.add1(1))
