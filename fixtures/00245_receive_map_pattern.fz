#---
# purpose: a map pattern in receive matches the correct message among several queued messages
#---
def main() do
  me = self()
  send(me, :skip)
  send(me, %{name: 42, age: 30})
  v = receive do
    %{name: n} -> n
  end
  dbg(v)
end
