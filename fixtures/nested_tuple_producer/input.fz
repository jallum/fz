fn pair(x) do
  send(self(), x)
  y = receive do
    z -> z
  end
  {x, y}
end

fn main() do
  result = {1, pair(2)}
  assert(result == {1, {2, 2}}, "nested tuple producer keeps tuple DP live across the receive continuation")
end
