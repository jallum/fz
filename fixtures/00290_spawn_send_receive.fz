#---
# purpose: spawn + send + selective receive delivers the message to the waiting parent
#---
def child(), do: send(1, 42)
def main() do
  spawn(child)
  assert((receive do x -> x end) == 42, "selective receive delivers the child's message")
end
