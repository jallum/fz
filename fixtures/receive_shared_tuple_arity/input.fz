fn main() do
  send(self(), {:b, 20})
  send(self(), {:a, 10})

  x = receive do
    {:a, v} -> v + 1
    {:b, v} -> v + 2
    {:c, v} -> v + 3
  end

  y = receive do
    {:a, v} -> v + 1
    {:b, v} -> v + 2
    {:c, v} -> v + 3
  end

  dbg({x, y})
end
