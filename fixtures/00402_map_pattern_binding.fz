#---
# purpose: a pattern matching a map key binds the hit value's type distinctly from the absent-key atom branch.
#---
def pick(%{id: x}), do: x
def pick(:none), do: :none
def pick(_), do: :unreachable

def main() do
  {pick(%{id: 1}), pick(:none)}
end
