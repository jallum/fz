fn catch(_), do: :anything
fn catch(0), do: :zero

fn cmatch(v) do
  case v do
    _ -> :anything
    0 -> :zero
  end
end

fn main() do
  dbg(catch(0))
  dbg(catch(7))
  dbg(cmatch(0))
  dbg(cmatch(99))
end
