---
purpose: "`test()` inside a defmodule body"
paths: [jit]
kind: test
repl-skip: "fz-dt3.12 — kind:test fixtures use the test macro/runner; fz repl --script only runs program main/0"
---

# sample_tests_module

`test()` inside a defmodule body
