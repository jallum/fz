fn main() do
  good = <<104, 105>>           # "hi" as raw bytes
  bad  = <<0xff, 0xff>>         # invalid UTF-8 sequence
  print(Utf8.valid?(good))
  print(Utf8.valid?(bad))
  print(Utf8.from_bytes(good))
  print(Utf8.from_bytes(bad))
end
