#---
# purpose: a map with a float key allocates exactly one map object; whether the float is separately boxed is path-variant
#---
def main() do
  m = %{3.14 => :ok}
  dbg(m)
  stats = Process.heap_alloc_stats()
  dbg(stats[:map_allocs])
  dbg(stats[:scalar_box_allocs])
end
