#---
# purpose: a macro that quotes a call to another macro re-expands the inner macro's result
#---
defmacro m1(x) do quote do: unquote(x) + 1 end
defmacro m2(x) do quote do: m1(unquote(x)) end
def main() do
  assert(m2(40) == 41, "m2 re-expands its quoted call to m1")
end
