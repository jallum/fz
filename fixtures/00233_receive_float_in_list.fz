#---
# purpose: a receive pattern matches a float nested inside a list
#---
def main() do
  send(self(), [2.5])
  assert(receive do
    [2.5] -> 7
  end == 7, "the [2.5] pattern matches the sent message")
end
