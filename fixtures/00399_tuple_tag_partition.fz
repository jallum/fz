#---
# purpose: a pattern matching on a tuple's leading atom tag selects the clause for that tag.
#---
def pick({:ok, x}), do: x
def pick({:error, reason}), do: reason

def main() do
  {pick({:ok, 1}), pick({:error, :bad})}
end
