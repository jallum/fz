defmodule Outer do
  fn f(x), do: x

  defmodule Inner do
    fn f(x), do: x + 1000
  end

  fn use_inner(x), do: Inner.f(x)
end

fn main() do
  dbg(Outer.f(7))
  dbg(Outer.Inner.f(7))
  dbg(Outer.use_inner(7))
end