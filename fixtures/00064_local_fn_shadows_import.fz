#---
# purpose: a local function definition shadows an imported name of the same arity.
#---
defmodule Math do
  def add(x, y), do: x + y
end
defmodule User do
  import Math
  def add(x, y), do: x - y
  def use_local(), do: add(10, 4)
end

def main(), do: dbg(User.use_local())
