#---
# purpose: a pattern matching on tuple size selects the clause for that arity.
#---
def pick({:one, x}), do: x
def pick({:two, x, y}), do: {x, y}
def pick(:other), do: :other

def main() do
  {pick({:one, 1}), pick({:two, 1, 2}), pick(:other)}
end
