#---
# purpose: a spawned closure captures its parent's value and relays it back through send/receive
#---
def parent(tag) do
  spawn(fn () -> send(1, tag) end)
  receive do
    x -> x
  end
end

def main() do
  assert(parent(99) == 99, "spawned closure captures tag and relays it back")
end
