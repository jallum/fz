fn f(v) do
  case v do
    {:ok, x} -> x
    :err -> 0
  end
end

fn g(v) do
  with {:ok, x} <- v do x else :err -> 0 end
end

fn main() do
  dbg(f({:ok, 7}))
  dbg(f(:err))
  dbg(f(:err))
  dbg(f({:ok, 7}))
  dbg(g({:ok, 7}))
  dbg(g(:err))
end
