---
purpose: "`test()` macro from the prelude — assert_eq / assert_neq / assert"
paths: [jit]
kind: test
repl-skip: "fz-dt3.11 — kind:test fixtures use the test macro/runner; fz repl --script only runs program main/0"
---

# sample_tests

`test()` macro from the prelude — assert_eq / assert_neq / assert
