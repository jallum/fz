#---
# purpose: an alias expands a short module name to its full dotted path at call sites.
#---
defmodule Long do
  defmodule Path do
    def f(x), do: x
  end
end
defmodule User do
  alias Long.Path
  def caller(), do: Path.f(7)
end

def main(), do: dbg(User.caller())
