defmodule M do
  fn double(x), do: x * 2
  fn quad(x), do: double(double(x))
end

defmodule N do
  fn helper(x), do: x + 100
end

fn main() do
  dbg(M.double(21))
  dbg(M.quad(5))
  dbg(N.helper(7))
end