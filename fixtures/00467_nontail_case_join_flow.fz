#---
# purpose: a non-tail case with a call arm flows its result into the enclosing expression
#---
def pick(x, fun) do
  next = case x do
    0 -> fun.(x)
    _ -> x
  end
  [next]
end
def main() do dbg(pick(0, fn (x) -> x + 1 end)) end
