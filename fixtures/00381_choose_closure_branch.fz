#---
# purpose: an if condition built from a higher-order closure call is folded to a direct branch rather than an indirect call.
#---
def choose(f, a, b) do
  if f.(a, b) do
    a
  else
    b
  end
end
def main(), do: choose(fn (x, y) -> x <= y end, 1, 2)
