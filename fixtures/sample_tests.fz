test(:test_addition) do
  assert_eq(1 + 1, 2)
  assert_eq(40 + 2, 42)
end

test(:test_atoms_compare) do
  assert_eq(:ok, :ok)
  assert_neq(:ok, :error)
end

test(:test_truthiness) do
  assert(true)
end

fn helper(x), do: x * 2

test(:test_calls_helper) do
  assert_eq(helper(21), 42)
end
