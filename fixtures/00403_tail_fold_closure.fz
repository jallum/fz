#---
# purpose: a tail-recursive fold over a list, driven by a closure reducer, converges to a concrete integer return type.
#---
def go(list, acc, f) do
  case list do
    [] -> acc
    [h | t] -> go(t, f.(h, acc), f)
    _ -> acc
  end
end

def myreduce(list, acc, f) do
  go(list, acc, f)
end

def main() do
  dbg(myreduce([1, 2, 3], 0, fn (x, a) -> x + a end))
end
