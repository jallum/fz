#---
# purpose: @type aliases declared inside a defmodule are parsed, attached, and resolved.
#---
defmodule M do
  @type id :: integer
  @type pair :: {id, id}
  @type keyword(t) :: [{atom, t}]
  def one(), do: 1
end

def main(), do: dbg(M.one())
