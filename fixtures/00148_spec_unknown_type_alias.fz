#---
# purpose: an @spec naming an unknown type alias surfaces a diagnostic but does not block compilation
#---
defmodule M do
  @spec one(unknown_thing) :: integer
  def one(_), do: 1
end
def main(), do: dbg(M.one(0))
