#---
# purpose: an if with a comparison condition routes correctly on both arms
#---
def helper(), do: 7
def pick(n) do if n > 0, do: helper(), else: 99 end
def main() do
  dbg(pick(5))
  dbg(pick(0))
end
