#---
# purpose: a zero-capture lambda passed as a value calls correctly
#---
def apply(f, x), do: f.(x)
def main() do
  f = fn (x) -> x end
  assert(apply(f, 41) == 41, "a zero-capture lambda applies correctly")
end
