#---
# purpose: Enum.reduce over a list with a user-supplied closure converges its accumulator to a concrete integer type.
#---
def main() do
  dbg(Enum.reduce([1, 2, 3], 0, fn (x, a) -> x + a end))
end
