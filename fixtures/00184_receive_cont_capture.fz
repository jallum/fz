#---
# purpose: a receive clause's bound capture keeps its typed value and flows into the enclosing tuple's return
#---
def parent(tag) do
  msg = receive do
    x -> x
  end
  {tag, msg}
end

def main() do
  result = parent(7)
  {result}
end
