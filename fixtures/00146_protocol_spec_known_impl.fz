#---
# purpose: an @spec with a protocol-domain parameter type accepts an argument whose type has a known impl
#---
defprotocol Enumerable do
  def reduce(enumerable, acc, reducer)
end

defimpl Enumerable, for: List do
  def reduce(list, acc, reducer), do: acc
end

defmodule M do
  @spec use(Enumerable.t(integer)) :: integer
  def use(xs), do: 1
end
def main(), do: dbg(M.use([1]))
