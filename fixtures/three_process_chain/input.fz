fn first_relay() do
  send(3, receive do x -> x + 1 end)
end

fn second_relay() do
  send(1, receive do x -> x + 1 end)
end

fn main() do
  spawn(first_relay)
  spawn(second_relay)
  send(2, 40)
  dbg(receive do x -> x end)
end
