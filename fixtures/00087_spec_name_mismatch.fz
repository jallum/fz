#---
# purpose: an @spec whose name does not match the following function's name is a parse-time error.
# expect: diagnostic
# diagnostic.code: parse/spec-name-mismatch
#---
defmodule M do
  @spec other(integer) :: integer
  def add1(n), do: n + 1
end

def main(), do: dbg(M.add1(1))
