#---
# purpose: spawn/1 receives a typed closure with captures directly, without going through a synthetic thunk function.
#---
def parent(tag) do
  spawn(fn () -> send(1, tag) end)
  receive do
    x -> x
  end
end

def main() do
  assert(parent(99) == 99, "spawned closure captures tag and relays it back")
end
