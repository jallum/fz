#---
# purpose: a function reached through a named reference dispatches its internal pattern match per the concrete activation.
#---
def pick(:left), do: :one
def pick(:right), do: :two
def pick(_), do: :other

def main() do
  f = &pick/1
  {f.(:left), f.(:right)}
end
