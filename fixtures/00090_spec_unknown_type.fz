#---
# purpose: an @spec referencing an unknown type name resolves with a diagnostic naming the unknown type, rather than a fatal error.
#---
defmodule M do
  @spec add1(unknown_thing) :: integer
  def add1(n), do: n + 1
end

def main(), do: dbg(M.add1(1))
