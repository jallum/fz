#---
# purpose: a macro built from quote and unquote expands to the arithmetic it quotes at compile time
#---
defmacro plus_one(x) do
  quote do: unquote(x) + 1
end
def main() do
  assert(plus_one(41) == 42, "quote/unquote macro expands to unquote(x) + 1")
end
