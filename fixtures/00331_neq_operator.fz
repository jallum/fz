#---
# purpose: != is the logical inverse of structural equality
#---
def main() do
  assert([1, 2] != [1, 3], "!= is true for structurally different lists")
  refute([1, 2] != [1, 2], "!= is false for structurally equal lists")
end
