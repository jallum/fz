#---
# purpose: a variadic extern call refuses a float argument because it is not an integer or pointer value
# expect: diagnostic
# diagnostic.code: artifact/incomplete-semantic-plan
#---
extern "C" defp libc::printf(fmt :: c_string, ...) :: c_int
def main() do libc::printf("%f", 1.5) end
