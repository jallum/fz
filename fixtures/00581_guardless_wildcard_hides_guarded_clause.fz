#---
# purpose: a guardless wildcard clause always matches first, so a later clause guarded by `when true` can never run
#---
def k(_), do: :first
def k(_) when true, do: :second
def main(), do: assert(k(1) == :first, "the guardless wildcard clause matches before the guarded one is tried")
