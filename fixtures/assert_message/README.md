---
purpose: "assert/2 + refute/2 carry a caller-supplied message; the truthy path returns nil cleanly across all four paths"
paths: [jit, interp, aot, repl]
---

# assert_message

assert/2 + refute/2 carry a caller-supplied message; the truthy path returns nil cleanly across all four paths.
