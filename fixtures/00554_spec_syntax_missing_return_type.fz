#---
# purpose: an @spec with a missing return type after `::` is a parse-time error.
# expect: diagnostic
# diagnostic.code: parse/expected-token
#---
defmodule M do
  @spec add1(integer) ::
  def add1(n), do: n + 1
end

def main(), do: dbg(M.add1(1))
