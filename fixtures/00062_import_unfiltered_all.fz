#---
# purpose: an unfiltered import brings all of a module's exported names into scope.
#---
defmodule Math do
  def add(x, y), do: x + y
  def mul(x, y), do: x * y
end
defmodule User do
  import Math
  def run(x, y), do: add(x, y)
end

def main(), do: dbg(User.run(2, 3))
