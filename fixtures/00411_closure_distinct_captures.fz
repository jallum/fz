#---
# purpose: a closure instantiated with distinct captured values specializes per capture rather than falling back to an any-key body.
#---
def add_to(x), do: fn (y) -> x + y end
def main() do
  f = add_to(7)
  g = add_to(3.5)
  dbg(f.(1))
  dbg(g.(2.0))
end
