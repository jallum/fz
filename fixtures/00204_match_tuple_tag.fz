#---
# purpose: same-arity tuples partition by their atom tag, each clause projecting a different field
#---
def pick({:ok, x}), do: x
def pick({:error, reason}), do: reason

def main() do
  {pick({:ok, 1}), pick({:error, :bad})}
end
