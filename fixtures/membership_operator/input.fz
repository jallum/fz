fn main() do
  xs = [1, 2, 3]
  range = 1..7//2
  map = %{2 => :two, 1 => :one}

  dbg(2 in xs)
  dbg(4 in xs)
  dbg(5 in range)
  dbg(6 not in range)
  dbg({1, :one} in map)
  dbg({3, :three} not in map)
end
