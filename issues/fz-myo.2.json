{
  "assignee": "",
  "blocked_by": [
    "fz-myo.1"
  ],
  "blocks": [],
  "created": "2026-07-09T19:48:09Z",
  "description": "append/quicksort/reverse have a real contract of OUTPUT + ALLOCATIONS; reductions_remaining is a compiled-call-graph-shape metric that drifts on unrelated specialization refactors (see parent root cause). Pinning it as a strict golden causes a recurring rebless treadmill. Replace the raw pin with EITHER (a) a derived invariant computed from the algorithm+input (e.g. back-edge count == number of tail-recursive element traversals for the fixed input) that still catches genuine over-specialization regressions, OR (b) telemetry-only status (as 2479a69fd did for the enum allocation regression). Do NOT simply delete the signal -- it is a genuine correctness signal (more yield-check work for identical semantics); preserve its regression-catching value while removing the shape-churn false-positives. Likely wants the bisect child done first to know if there is a real regression hiding under the drift.",
  "id": "fz-myo.2",
  "labels": [],
  "parent": "fz-myo",
  "comments": [
    {
      "text": "UNBLOCKED by fz-myo.1's completed bisect. The decision input is now factual: the duplication that caused jump #2 is already fixed at HEAD (1 activation each) while the elevated counts persist as a property of correct back-edge classification (per-iteration yield-check on self-recursive loops). FIX-THEN-LOWER IS OFF THE TABLE -- no waste remains. Implement the de-pin: remove reductions_remaining from the strict goldens for append/quicksort/reverse (their contract is output+allocations) and add EITHER a derived invariant (burned = f(recursive iterations) -- quicksort 94 = 29 partition + 22 qsort + constants at the fixed input) OR telemetry-only with an upward-drift alarm. Current pinned values (quicksort 3906/94, append 3997/3, reverse 3990/10) are the baseline. PROCESS NOTE worth carrying: the culprit was an un-ticketed 2949-line WIP checkpoint -- the attribution cost of landing un-ticketed WIP on the integration branch is now measured (two reblesses + three investigations).",
      "timestamp": "2026-07-15T15:09:06Z"
    }
  ],
  "priority": 3,
  "status": "open",
  "title": "fz-myo: stop pinning raw reductions_remaining as a strict golden for output+allocation-contract fixtures -- derive it or make it telemetry-only",
  "type": "bug",
  "updated_at": "2026-07-15T15:09:06Z"
}
