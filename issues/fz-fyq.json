{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-05-18T20:01:21Z",
  "description": "## The arc\n\nQuicksort needs destructure. Destructure needs a clean answer to \"this synthesized branch can't fire\" — which today is three not-quite-aligned mechanisms (`is_synthetic_match_error_block`, `find_emptied_var`, `ir_fold` cond-singleton). Cleaning that up surfaces that pattern-bind exhaustivity is one instance of a broader fact: **dead branches in the CFG that the typer can prove**. The same fact is needed for fn-clause dispatch's `:function_clause` halts, for user `case` arms with provably-empty scrutinees, and for type-test folding. One data model serves all of them.\n\nQuicksort is the closing fixture: `{lo, hi} = partition(...)` on the hot path of a recursive sort. Its `expected.clif` shows projection-only lowering — visible proof the data model + fold work as intended.\n\n## Data model\n\n- **`module.source.branch_origins: HashMap\u003c(FnId, BlockId), BranchOrigin\u003e`** (side-table, mirrors `term_span`'s shape). Lowering tags every `Term::If` it generates for pattern-bind or fn-clause failure machinery. Absence = `User`.\n- **`ModuleTypes::dead_branches: HashMap\u003c(FnId, BlockId), DeadBranch\u003e`** where `DeadBranch ∈ {Then, Else}`. Populated by ir_typer after typing all specs, by intersecting per-spec `find_emptied_var` results. Cross-spec consensus required for soundness.\n\n## DAG\n\n    E1 → E2 → E3 ┐\n              └→ E4 ┐\n                    ├→ E6 → E7\n    E5 ───────────┘\n\n- **E1** — `BranchOrigin` side-table + ir_lower populates it. Data model only, no behaviour change.\n- **E2** — `ModuleTypes::dead_branches` published by ir_typer; cross-spec intersection of `find_emptied_var`. No consumers yet.\n- **E3** — Diagnostic consumes `dead_branches` filtered by `BranchOrigin::User`. Retires `is_synthetic_match_error_block`. Unblocks destructure on every path.\n- **E4** — `ir_branch_fold` inside `ir_codegen::compile`'s post-type chain. Folds `Term::If → Term::Goto` for any branch in `dead_branches`. Retires `ir_fold`'s cond-singleton If fold (subsumed). Quicksort's CLIF goes projection-only.\n- **E5** — Interp parity: list primitives (`MakeList`, `ListCons`, `ListHead`, `ListTail`, `IsEmptyList`). Independent — interp runs raw IR and was silently \"Deferred\" for any program with a list literal.\n- **E6** — Fixture-matrix parity for destructuring across all four legs.\n- **E7** — `fixtures/quicksort/` as the closer.\n\n## Invariants\n\n- **BEAM-conform**: `[h|t] = []` is a runtime `match_error`, not a compile-time refusal. The fold only fires when the typer proves the branch dead under every live spec.\n- **Three-path parity**: interp, JIT, AOT, plus repl leg.\n- **Soundness**: cross-spec consensus before a fold. Some spec leaving a branch live keeps that branch.\n- **Vocabulary discipline**: `CallsiteOutcome` stays a verdict about an action. `dead_branches` is a type-fact, lives on `ModuleTypes` alongside `block_envs` and `vars`. Different kinds of thing, different homes.",
  "id": "fz-fyq",
  "labels": [],
  "priority": 2,
  "status": "open",
  "title": "Destructuring: data-model-up, with quicksort as the closing fixture",
  "type": "epic",
  "updated_at": "2026-05-18T20:40:08Z"
}
