{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-05-18T17:21:34Z",
  "description": "The fz-9pr arc made callsite outcomes first-class data, and then `fz dump --emit outcomes` made the model legible. Looking at real dumps surfaced three design seams the model still papers over.\n\nThis epic closes them in types, not edge-case code.\n\n## The three seams\n\n### 1. Descr lattice has outgrown `Const`\n\nThe reducer judges in lattice-space (`fold_make_closure`, `fold_make_tuple` produce `closure_lit`/`tuple_lit` Descrs) but materializes in IR-space (`Const` = scalars only). Where they don't line up, the fold silently dies — the result is computed and thrown away because `is_scalar_literal` (ir_reducer.rs:500) rejects it and `literal_to_const` (ir_reducer.rs:513) has no arm.\n\nConfirmed gaps: `closure_lit` (e.g. `fixtures/closure_typed_captures` — `add_to(10,20)` survives as Emitted), `tuple_lit` (any fn returning `{:ok, n}` etc.). Empty list (`[]`, now a zero-alloc sentinel via fz-s9y) fits the same pattern.\n\nFix: one `descr_to_materialize(d, env) -\u003e Option\u003cVec\u003cStmt\u003e\u003e` that owns the full vocabulary. Reducer asks one question, gets one answer.\n\n### 2. Outcomes are snapshots when they should be traces\n\n`CallsiteOutcome::Emitted { target }` records where we ended up, not how we got there. The typer promotes `Stalled { reason }` → `Emitted { target }` and drops the reason on the floor — exactly the info needed to answer the central debugging question, \"why didn't this fold?\"\n\nFix: `Emitted { target, came_from: Option\u003cStalledReason\u003e }`. Trace-shaped, like provenance chains.\n\n### 3. Producers don't tag outcome context\n\nThe reducer publishes outcomes for every body it walks (User, Prelude, fn_clause_N, lambda_N, k_*, etc.) without distinguishing them. The typer separately computes reachability (`FnTypes.reachable_blocks`) but doesn't tag outcomes. `dump_outcomes_pipeline` tries to stitch three truths at print time. `compute_survivors` in main.rs is a third independent computation of the same idea for pattern_check.\n\nFix: `FnIr.category: FnCategory` set at lowering. Typer's reachability lifted to module-level. Dump filters tagged data instead of stitching. `compute_survivors` deleted.\n\n## What dies\n\n- `literal_to_const` as a separate helper (folded into materializer).\n- `is_scalar_literal`'s narrow gate (widened or replaced by materializer query).\n- `compute_survivors` in main.rs.\n- Three-truths-stitching in `dump_outcomes_pipeline`.\n\n## The DAG\n\n```\nArc 1 (sequential):    .1 → .2 → .3\nArc 2 (single):        .4\nArc 3 (mostly parallel): .5, .6 → .7 → .8\n```\n\n8 tickets, one commit each.\n\n## Out of scope\n\n- Enabling list literal folding (`fold_prim` skips `Prim::MakeList` at reducer.rs:188 because `list_of(elem)` loses length info — needs IR-walking per the RED.3+ deferred comment). Filed separately.",
  "id": "fz-f88",
  "labels": [],
  "priority": 1,
  "status": "open",
  "title": "Reducer + outcomes simplification — close the lattice/IR and producer/consumer gaps",
  "type": "epic",
  "updated_at": "2026-05-18T17:21:34Z"
}
