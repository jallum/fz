{
  "assignee": "Jason Allum",
  "blocked_by": [],
  "blocks": [
    "fz-bfh.5.5"
  ],
  "created": "2026-06-23T22:41:19Z",
  "description": "The value of lifting the SemanticClosed barrier (fz-bfh.5.5 steps 2-4: co-settling transport pre-seal) is incremental-recompilation blast-radius -- and NO test exercises it. The engine's rebase/contribution semantics are tested generically (scheduler_test.rs:615+: cumulative ascent vs replacing-rebase vs retraction), but nothing edits a source, re-drives the SAME world, and asserts which transport facts (ExecutableTransport(E)/TransportPlan) re-settle vs stay cached. The full matrix + shadow + contract tests all run a single batch drive to a sealed closure.\n\nBuild that signal: a harness that drives a root to settle, applies a localized source edit, re-drives, and asserts (via telemetry/fact revisions) that only the affected executables' transport facts re-settled. Without it, steps 2-4 of fz-bfh.5.5 produce a byte-identical BATCH plan (verifiable) but their actual purpose -- pre-seal/incremental co-settling -- is unverifiable, so landing the contribution-fact machinery would be non-load-bearing complexity until an unverifiable gate-drop. This is northstar 'add measurements for signal' and is really fz-rh2 (incremental spine) territory. Blocks the value-bearing remainder of fz-bfh.5.5.",
  "id": "fz-g2f",
  "labels": [],
  "comments": [
    {
      "text": "DESIGN (investigated, code-grounded):\n- NO source-edit/invalidate primitive exists: world.submit_code -\u003e code.define always mints a NEW CodeId; there is no redefine/replace_code/invalidate. So the signal cannot be 'edit function F'. It must use incremental ADDITION that grows an EXISTING root's closure (production path), or a new production edit primitive must be built first (fz-rh2 spine).\n- Observable: per-job telemetry span [\"fz\",\"compiler2\",\"job\"] (drive.rs:193) carries metadata job=opaque_debug(\u0026job). Capture + filter metadata_str(event,\"job\").starts_with(\"DeriveExecutableTransport\") and count per drive() call. fact_revision(key) (world.rs:2574) is the content-revision signal (secondary; facts are content-addressed so unrelated content revs may already be stable).\n- RED-\u003eGREEN mechanism (traced): derive_executable_transport(E) currently READS SemanticClosed(root). Growing the closure re-runs SealSemanticClosure -\u003e define_semantic_closure changed=true -\u003e SemanticClosed revision bumps -\u003e scheduler rebases ALL readers -\u003e every DeriveExecutableTransport(E) re-runs (RED: unaffected execs re-derive). After the lift (5.5 step 4) the per-exec job reads only E's own prereqs (ActivationAnalyzed/RuntimeDemand/InputSources contributions), so unrelated growth rebases nothing -\u003e unaffected execs' transport jobs do NOT re-run (GREEN).\n- Test shape: drive 1 settles root main (capture DeriveExecutableTransport run-set S1); submit additional code that grows main's SAME closure; drive 2 (capture run-set S2); assert an executable provably unaffected by the addition is in S1 but NOT in S2.\n- OPEN design crux: construct a closure that cleanly SETTLES then GROWS via production primitives. New-root or two-roots-sharing-a-helper does NOT exercise it (ExecutableTransport(E) is root-independent; a new root's growth doesn't bump the old root's SemanticClosed). Needs main to NEWLY reach an executable on the second drive -- candidate mechanisms: deferred function definition (call present in drive 1 but definition submitted in drive 2) or an incrementally-added defimpl that an existing polymorphic call site newly resolves to. Verify the drive-1 state settles (TransportPlan present) with the not-yet-reached target.",
      "timestamp": "2026-06-23T22:55:27Z"
    },
    {
      "text": "DEEPER FINDING (gates feasibility): in the current closed-world model a SETTLED root closure may be STABLE under code addition -- if the root reaches the new code it could not have settled without it; if it does not reach it, the closure does not grow. Closure growth happens DURING the initial drive (code discovery), not across drives after settling. Combined with the absence of any edit/invalidate primitive, this means incremental recompilation may have NO production trigger today -- so the signal (and 5.5's co-settling value) may be unmeasurable until fz-rh2 builds an incremental-edit capability.\nTHE ONE OPEN EMPIRICAL QUESTION that decides feasibility: does incrementally adding a defimpl RE-TRIGGER an already-settled consumer's call-site resolution (CallSiteSummary subscribing to the protocol's impl-set), thereby growing the settled root's closure? If YES, fz-g2f is buildable via incremental defimpl addition (open-world-ish dispatch fixpoint). If NO, fz-g2f is blocked on a new incremental-edit primitive (fz-rh2 spine) and 5.5 steps 2-4 cannot be value-verified until then. Resolve this before writing the test.",
      "timestamp": "2026-06-23T22:56:40Z"
    },
    {
      "text": "FEASIBILITY CONFIRMED (YES): incrementally adding a defimpl on a later drive DOES re-trigger an already-settled consumer's call-site resolution, via the FactKey::ProtocolDispatch(protocol) subscription -- resolve_protocol_call reads it (jobs/semantic.rs:1251); a new defimpl bumps it (source_publish.rs:1218 refresh_protocol_dispatch -\u003e changed); it is NOT cumulative (drive.rs:88-98) so the change is a ground-shift that rebases subscribers (scheduler.rs:151-176, 217-229). So the settle-then-grow trigger = incremental defimpl addition where main's polymorphic/value-joined call site newly dispatches to the added impl. fz-g2f is buildable. Test plan: drive 1 settle main (protocol call, one impl, value joined over two types so dispatch is open); capture DeriveExecutableTransport run-set; drive 2 add the second defimpl -\u003e ProtocolDispatch bumps -\u003e main's closure grows -\u003e assert an unrelated executable's DeriveExecutableTransport is NOT in drive-2's run-set (RED today via SemanticClosed rebase-all; GREEN after 5.5 step 4).",
      "timestamp": "2026-06-23T23:00:35Z"
    },
    {
      "text": "DONE (c6fa7cb93). Signal built + GREEN: executable_transport.derived telemetry event in define_executable_transport + in-process test compiler2_adding_a_defimpl_reprojects_only_the_cone_its_dispatch_reaches. Drives a root to settle (Shout protocol, one Integer impl, value-joined say call in shouty), adds defimpl for String on drive 2 (ProtocolDispatch bump grows the closure via the production open-world path), asserts lonely (cone {lonely,quiet}, disjoint from perturbed shouty/main) is NOT re-derived. RED at HEAD (seal bump rebased all transport readers); GREEN after fz-bfh.5.5's cone-scoped subscription.",
      "timestamp": "2026-06-24T18:39:48Z"
    }
  ],
  "priority": 3,
  "status": "in_progress",
  "title": "Incremental-recompilation signal: test transport facts re-settle with bounded blast-radius",
  "type": "task",
  "updated_at": "2026-06-24T18:39:48Z"
}
