{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-09-07T12:21:01Z",
  "description": "Found while measuring fz-5xp.55.\n\nWalking 640 bytes with\n\n    fnp walk(\u003c\u003c_c, r :: binary\u003e\u003e, n), do: walk(r, n + 1)\n\nallocates 1151 ProcBin stubs. There are 576 shareable tails on that walk, so\nthe count is exactly 2x -- one stub per tail, twice.\n\nAn eprintln in fz_bs_read_field_bits confirms it directly: two identical\nextractions per step, same pos, same needed_bits, same source.\n\n    PROBE bs_read binary pos=8 needed=1272 bit_len=1280 tail=true\n    PROBE bs_read binary pos=8 needed=1272 bit_len=1280 tail=true\n    PROBE bs_read binary pos=8 needed=1264 bit_len=1272 tail=true\n    PROBE bs_read binary pos=8 needed=1264 bit_len=1272 tail=true\n\nWHERE TO LOOK: there are two callers of fz_bs_read_field_ref in the interp\nleg -- src/ir_interp/dispatch_exec.rs:646 and src/ir_interp/backend.rs:1591.\nThe shape fits: the dispatch plan reads the fields to decide whether the\nclause matches, throws the extracted values away, and then the body reads\nthem again to bind. Not verified which pair it is; that is the first step.\n\nWHY IT MATTERS NOW: before fz-5xp.55 this was invisible, because the copy\ncost dominated and the doubling was a constant factor on a quadratic. With\ntails shared, the two stubs per byte ARE the remaining cost of a scan, and\nhalving them halves it.\n\nThis is another instance of the epic's recurring pattern (fz-5xp.21): one\nquestion -- 'what is this field' -- answered twice, in two places, with the\nallocation happening in both.",
  "id": "fz-5xp.56",
  "labels": [],
  "parent": "fz-5xp",
  "comments": [
    {
      "text": "MEASURED PRECISELY, and the description's 'twice' is right but undersells it.\n\nProbing both interp read sites on a 5-byte walk of `\u003c\u003c_c, rest :: binary\u003e\u003e`:\n\n    P DISPATCH read      \u003c- field 0, the integer\n    P DISPATCH read      \u003c- field 1, the binary tail\n    P BODY read          \u003c- field 0 again\n    P BODY read          \u003c- field 1 again        ... x5 steps\n\nFOUR reads per step for a two-field pattern. Each FIELD is read twice: once by\nthe dispatch plan deciding whether the clause matches, and once by the body\nbinding the variables. The tail is a field, so that is two tail allocations per\nstep, which is where the 2x stub count comes from.\n\nTHE MECHANISM TO AVOID IT ALREADY EXISTS AND IS ALREADY POPULATED. Dispatch\nstores what it extracted:\n\n    state.bitstring_fields.insert((subject, index), extracted);\n    apply_direct_bitstring_bindings(plan, field_subject, extracted, state);\n\nand the native emitter does the same in emit_bitstring_test. So the values the\nbody re-reads are already in hand at the moment it re-reads them. The body emits\nits own BitReadField prims regardless, because the lowering does not know the\ndispatch region covered the same pattern.\n\nSo this is not a runtime fix. It is the clause head being compiled TWICE -- once\ninto the dispatch matrix to decide the clause, once into the body to bind -- and\nthe second one is redundant exactly when the first one covered it. Related to\nfz-5xp.22 (fold the two native dispatch region emitters into one) and another\ninstance of fz-5xp.21.\n\nNEXT STEP: find where body lowering emits BitReadField for a clause-head pattern\nand whether it can see that the dispatch plan already projected that subject.\nNot attempted yet.",
      "timestamp": "2026-09-07T13:51:31Z"
    }
  ],
  "priority": 2,
  "status": "open",
  "title": "A bitstring field is extracted TWICE per clause match, so every scan step allocates two tails",
  "type": "bug",
  "updated_at": "2026-09-07T13:51:31Z"
}
