{
  "assignee": "Jason Allum",
  "blocked_by": [
    "fz-1ah.11"
  ],
  "blocks": [],
  "created": "2026-07-13T07:22:08Z",
  "description": "Goal\nEvery compiler2/fz2 production emitter passes existing raw semantic authorities and direct raw scalars. Handlers alone inspect, transform, or retain callback-scoped values. With no matching handler, telemetry performs no payload construction, traversal, transformation, or allocation, and NullTelemetry can erase the call path.\n\nProven model failures\nThe original guard trusted helper parameters without proving caller provenance, admitted computed locals when assignment, branch, or return use looked semantic, hand-listed sink APIs, missed UFCS and indirect function-item uses, and resolved helpers by names too loosely. TypeName and JobEffects were cloned or reconstructed at telemetry seams. Activation-input events repeated one completion per key. Runtime dbg output was semantic program output mis-modeled as telemetry: the original FFI callback was an indirect emitter helper, while deferring emission required a telemetry-only Vec copy.\n\nResolution\nThe guard now inventories payload-bearing Telemetry, TelemetryExt, Span, DetachedSpan, and ExecutionContext APIs; rejects eager payload calls; recognizes method and UFCS forms; and bans sink APIs or emitter helpers used as function items, fields, returns, or aliases. Its callable graph uses scan module, impl owner, name, and arity; owned scalar parameters recursively validate every direct callsite; borrowed parameters and recursively raw aliases terminate provenance; recursion, unknown callsites, computed locals, and semantic-use laundering fail. Qualified unresolved free-helper calls conservatively join known helper name and arity so direct and two-hop aliases cannot escape.\n\nWorld definition APIs borrow stable keys and clone only where semantic storage owns them. Scheduler completion carries raw JobCompletion and World; AppliedStep owns the output-key set; handlers derive reads, waits, changed facts, activation keys, and presentation data during dispatch. One activation-input event carries the whole completion. Tests select module identities through World rather than relying on HashSet order.\n\nProgram output is no longer telemetry. The runtime callback passes the existing rendered byte slice synchronously to an event-scoped OutputSink. Stdout writes immediately, retaining test sinks copy during the callback, and NullOutput does nothing. The fz.runtime.dbg event, handler, staging buffer, drains, and indirect-emitter exception are deleted.\n\nSignal\nDirect, recursive, unknown, computed-local, dead-store, branch, return, API-alias, helper-alias, UFCS, qualified direct, and qualified two-hop fixtures pin the guard. Raw pointer identity proves no output copy precedes the sink callback; retaining and null sink lifetimes are pinned; interpreter, JIT, CLI stdout, and AOT stdout parity pass. cargo test --lib passes 1,411 tests with 6 ignored and no failures. Runtime output tests pass 4/4. cargo fmt --all --check, workspace/all-target/all-feature check, strict workspace clippy, and cargo build --release --bin fz2 pass. The release fixture builds without diagnostics both with and without telemetry, both binaries exit zero, and JSON telemetry contains 1,055 events with no runtime.dbg event. No Rust comment block, lint allowance, shadow payload schema, telemetry-only counter, or retained staging buffer was added.",
  "id": "fz-1ah.12",
  "labels": [],
  "parent": "fz-1ah",
  "priority": 1,
  "status": "in_progress",
  "title": "Migrate raw-authority telemetry handlers and audit event payloads",
  "type": "bug",
  "updated_at": "2026-07-15T00:18:19Z"
}
