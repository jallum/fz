{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-07-07T16:33:07Z",
  "description": "SURFACED by the fz-h29 adversarial audit. fz-h29 made TOKEN spans self-describing (embedded the originating SourceId in the encoded token tuple, deleting the external decode-time code_id threading), closing the cross-splice mismatch by construction for tokens. The SAME class of hazard remains OPEN for AST-node meta spans decoded via span_from_meta.\n\nCURRENT STATE: span_from_meta (quoted_function.rs ~1755-1770, also quoted_surface.rs) builds Span::new(SourceId(ctx.code_id.as_u32()), start, start+length) from the CALLER-supplied ctx.code_id (DecodeCtx / SurfaceSourceContext). The AST node's __fz_span__ meta map carries only start/length -- NO code id of its own. So if a macro's quoted AST fragment (an Expr/Pattern node, not merely a token blob inside a TypeExprBody) is rematerialized into a caller's heap the way run_macro_on_source splices token blobs, any Expr/Pattern span decoded from that spliced node gets span{code_id: caller, offsets: macro-file} -- the exact latent mismatch fz-3sb/fz-h29 eliminated for tokens, now for general expression/pattern spans.\n\nPresumed still LATENT today for the same reason the original token bug was (nothing surfaces/type-checks against those AST-meta spans yet), but it becomes OBSERVABLE the instant an Expr/Pattern meta span is surfaced across a macro splice.\n\nCORRECT-BY-CONSTRUCTION FIX (the natural fold, mirroring fz-h29): embed the originating code_id INTO the __fz_span__ meta-span encoding at emit time, so span_from_meta needs NO external ctx.code_id and a spliced AST node keeps the code id of the file that owns its offsets. This would also let DecodeCtx.code_id / SurfaceSourceContext.code_id be retired entirely (fz-h29 kept them ONLY because span_from_meta still needs them) -- a further fold/reduce win. Analogous to fz-s2b (closed not-reachable for tokens); this is its AST-span sibling. Related: fz-h29, fz-3sb, fz-s2b.",
  "id": "fz-5vt",
  "labels": [],
  "comments": [
    {
      "text": "Folded at 11bc7b510. Adversarial audit clean (no correctness defect): meta-map format change verified (3 fields by KEY not position, both span_from_meta readers + frontdoor_test unaffected, no persistence hazard, code_id baked from Span.code_id set at lex time), DecodeCtx/SurfaceSourceContext FULLY deleted (both carried only code_id; zero dangling refs tree-wide), ~60 de-threaded functions each used ctx only to forward to span_from_meta. ONE finding applied at fold: the dead _code_id param on derive_function_surface was a test-only affordance (never read; test injected a 'wrong' value into an unread slot). Fixed by collapsing the whole signature -- _code_id + also-dead _code_name + _tel (sole caller is a direct call, no trait/fn-pointer constraint) -\u003e derive_function_surface(root: \u0026QuotedSourceRoot). Both cross-caller tests restructured to the honest single-CodeId shape (renamed a_decoded_{token,ast_node_meta}_span_carries_its_own_files_baked_code_id); non-vacuous confirmed by sabotage (baking constant 0 fails ONLY the AST-meta test, token test correctly unaffected). Net -191 lines. Verified on branch: lib 1242/0/6, matrix 19 known-red, clippy+fmt clean. Docs .agent/docs/quoted-source.md present-tense (3-field self-describing __fz_span__). Closes the cross-splice hazard for AST-node spans by construction, mirroring fz-h29 for tokens, and retires the last external-code_id decode dependency.",
      "timestamp": "2026-07-07T18:40:18Z"
    }
  ],
  "priority": 3,
  "status": "open",
  "title": "AST-node meta spans (span_from_meta) have the same cross-splice hazard fz-h29 fixed for tokens: __fz_span__ carries no code_id, so a macro-spliced Expr/Pattern span gets caller code_id + macro-file offsets",
  "type": "bug",
  "updated_at": "2026-07-07T18:40:18Z"
}
