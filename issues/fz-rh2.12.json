{
  "assignee": "",
  "blocked_by": [],
  "blocks": [
    "fz-rh2.9"
  ],
  "closed_at": "2026-06-10T08:00:29Z",
  "created": "2026-06-08T20:50:22Z",
  "description": "Goal: compiler2 owns its type world end-to-end. A type name is an identity resolved to a hard Ty by a DEMANDED fact, exactly like an MFA; once a hard Ty is in hand, only the shared no-risk algebra (crate::types) and spec engine (crate::specs) are used. Sever the reincorporation of old-world naming -- the per-call ModuleTypeEnv rebuilt by re-lexing runtime source (function_type_env -\u003e runtime_library::{root_type_env,interfaces}), the eager protocol-domain union, and the name-keyed Nominals leak. None of these match the northstar; they were copied in wholesale.\n\nSignal: (1) the drive lexer.pass count equals the number of distinct sources -- the ~117 redundant re-lexes per compile that quicksort.log exposed are structurally gone and observably so; (2) the 150 compiler2 drive_test oracles stay green (reached frontier, Enumerable dispatch selection, unused_runtime_library_stays_cold, JIT/interp runs); (3) DeriveTypeDef (the expensive type-algebra) runs only for type names a reached consumer actually reads -- a @type no reached spec references stays cold like an uncalled function.\n\nStrategy: one decision in the right place -- type names ride the namespace + fact rails that already carry MFAs (reference_type mirrors reference_function; DeriveTypeDef mirrors DeriveFunctionContract; the work graph is the fixpoint). That makes the redundant work unrepresentable, so the inches are mostly deletions. The shared kernel (crate::type_expr, runtime_library, ModuleTypeEnv) is UNTOUCHED -- it stays for the retiring legacy fz frontend and dies with it. Boundary discriminator: do we have the hard Ty yet? Before = in-house; after = shared.\n\nDesign captured present-tense in .agent/targets/{type-naming,type-world,set-theoretic-types,protocols}.md and the northstar Type Naming And The Surface Tier section; promoted to .agent/docs/ at the end.\n\nLaces into fz-rh2 as a blocker of fz-rh2.9 (old compiler scrap): compiler2 must stand alone on in-house naming before the old world is scrapped. The terminal child returns to the original quicksort.log perf question -- by then solved by construction.",
  "id": "fz-rh2.12",
  "labels": [],
  "parent": "fz-rh2",
  "comments": [
    {
      "text": "Sequencing (one decision -\u003e mostly deletions; the work graph is the fixpoint):\n\n```mermaid\ngraph LR\n    1[\".1 note names + reference_type\"] --\u003e 2[\".2 DeriveTypeDef + TypeDefined\"]\n    2 --\u003e 3[\".3 in-house resolver\"]\n    2 --\u003e 6[\".6 inner-in-symbol kernel\"]\n    3 --\u003e 4[\".4 re-seat consumers; delete function_type_env\"]\n    3 --\u003e 5[\".5 protocol domain = marker\"]\n    4 --\u003e 7[\".7 sever + lexer-pass guard\"]\n    5 --\u003e 7\n    6 --\u003e 7\n    7 --\u003e 8[\".8 promote docs\"]\n    8 --\u003e 9[\".9 back here: re-examine quicksort.log\"]\n    9 -.lace.-\u003e RH29[\"fz-rh2.9 scrap old compiler\"]\n```\n\nBoundary: in-house = name -\u003e hard Ty (.1-.5); shared/untouched = the algebra and spec engine over hard Ty, plus the whole legacy crate::type_expr / runtime_library / ModuleTypeEnv (kept for the retiring fz frontend). Removes more than it adds.",
      "timestamp": "2026-06-08T20:52:30Z"
    },
    {
      "text": "Design refinement (from the @spec to FunctionContract parity discussion):\n\nThe lifecycle is Note / Reference / Resolve, and the reference/resolve split is the load-bearing invariant -- it is what makes the re-lex structurally unrepresentable rather than merely removed.\n\n  - Reference is an INDEX-time walk (.1): every type-position name is minted and recorded as its consumer dep. The recorded set IS the consumer wait-set.\n  - Resolve is split across the consumer and the resolver: the CONSUMER (.4) is the demand source -- it waits on its recorded TypeDefined, then calls a PURE-READ resolver (.3) that never blocks, demands, or mints.\n\nSo a type name rides one tier below the function that names it: a @spec referencing SomeModule.t pulls DeriveTypeDef only because DeriveFunctionContract waited on TypeDefined -- and a @type no reached consumer references stays cold (epic signal 3, gated in .2). DeriveFunctionContract over MFAs is the exact template.\n\nUnchanged by this: the DAG, the inch boundaries, the lace into fz-rh2.9, and the two-grains contract/body split (already existing compiler2 structure -- a caller reads the contract without entering the body).",
      "timestamp": "2026-06-08T21:06:24Z"
    },
    {
      "text": "Sequencing refined -- dark-launch + cut-over phases (from the \"test it without disrupting anything, then cut over\" strategy). New gate .10 inserted: the new path is proven end-to-end BEFORE any old machinery is deleted, in the one window where both paths coexist and can be diffed.\n\n```mermaid\ngraph LR\n    subgraph BUILD_DARK[\"build dark -- additive, every commit green\"]\n        1[\".1 note + reference walk\"] --\u003e 2[\".2 DeriveTypeDef + TypeDefined\"]\n        2 --\u003e 3[\".3 pure-read resolver\"]\n        2 --\u003e 6[\".6 inner-in-symbol kernel\"]\n    end\n    3 --\u003e 10[\".10 dark-launch PARITY proof\"]\n    6 --\u003e 10\n    subgraph CUT_OVER[\"cut over -- gated green by .10\"]\n        10 --\u003e 4[\".4 re-seat; delete function_type_env\"]\n        10 --\u003e 5[\".5 protocol domain = marker\"]\n    end\n    subgraph SEVER[\"sever + prove gone\"]\n        4 --\u003e 7[\".7 sever + lexer-pass guard\"]\n        5 --\u003e 7\n        6 --\u003e 7\n        7 --\u003e 8[\".8 promote docs\"]\n        8 --\u003e 9[\".9 back here: quicksort.log\"]\n    end\n    9 -.lace.-\u003e RH29[\"fz-rh2.9 scrap old compiler\"]\n```\n\nWhy .10 is its own inch: the parity-vs-old diff (new resolver Ty == function_type_env Ty) is only possible while both paths exist. .4 deletes function_type_env, so that diff has a one-shot window -- now. Its temporary half is removed by .4; its permanent half (firing + correct Ty) stays as oracle. Supersedes the earlier mermaid comment.",
      "timestamp": "2026-06-08T21:15:45Z"
    },
    {
      "text": "AMENDS STRATEGY (from the in-house-parse discussion). The strategy line \"the shared kernel (crate::type_expr ... ModuleTypeEnv) is UNTOUCHED\" is now too broad. Investigation of the type-expr parser shows the old-world PARSE is the wrong shape: it welds a ModuleTypeEnv into the grammar (env.get_alias decides name-vs-var and validates arity at parse time; type vars interned during parse). That cannot be reused without dragging the whole-program env back in.\n\nRefinement: the type-expr PARSE and NAME-RESOLUTION come in-house (new prequel .11). A name in a type = a reference bound as a type in the NAMESPACE at the declaration point; we capture that namespace (a hash-consed BindingId -- one u32) alongside the AST, and the namespace REPLACES ModuleTypeEnv as the resolution context (functions already capture via FunctionDef.namespace; @types capture at their point, reserved in the first define_scope walk so the build_module_type_env fixpoint is gone).\n\nStill UNTOUCHED and shared: crate::specs (the scheme engine over ResolvedSpec/ResolvedTypeShape), the type algebra over hard Ty, and runtime_library. Legacy crate::type_expr (parse_type_expr, ModuleTypeEnv, build_module_type_env) stays FROZEN for the dying fz frontend and is deleted with it in fz-rh2.9 -- not reused by compiler2.\n\nSequencing: .11 (in-house parse + namespace capture) is the new entry point, blocking .1. Full order: .11 -\u003e .1 -\u003e .2 -\u003e {.3, .6} -\u003e .10 (parity gate) -\u003e {.4, .5} -\u003e .7 -\u003e .8 -\u003e .9, laced before fz-rh2.9.",
      "timestamp": "2026-06-08T21:33:54Z"
    }
  ],
  "priority": 1,
  "status": "closed",
  "title": "Compiler2 type naming: bring name→Ty in-house as demanded facts",
  "type": "epic",
  "updated_at": "2026-06-10T08:00:29Z"
}
