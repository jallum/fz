{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-08-31T23:08:20Z",
  "description": "Measured while landing fz-kdt.104 (which dropped the arms a sibling COULD stand in for). What is left is the harder half of the same defect: dispatch arms whose runtime questions are literally the same question, where NO arm semantically covers the others, so no arm can be dropped without losing a body.\n\nMEASURED AT FIFO, 577-fixture corpus, post-fz-kdt.104 (instrumented run of callsite_dispatch::call_destinations): 12 distinct dispatch shapes across 10 fixtures, 18 indistinguishable arm pairs. Two sub-shapes:\n\n(1) NO QUESTIONS AT ALL (8 shapes: 00231_joined_fn_refs_enum_reduce, 00275_enum_count_member_reduce, 00277_enum_tier0_fixture, 00281_opaque_reducer_closure, behavior/enum_map_family, behavior/opaque_fn_value_join, behavior/range_enumerable, behavior/repr_seam_enum_count_after_reduce2). The arms differ only in closure IDENTITY at some position; runtime_type_test_envelope collapses funcs to fun_top, discriminating_inputs finds nothing, and the plan is a bare source-order fallthrough. This is the documented callable-correlation policy (callsite_dispatch.rs test callable_flow_dispatch_does_not_discriminate_unobservable_callable_correlations, and compiler2/dispatch_reachability agreeing with source-ordered selection), so it may be correct-by-policy rather than a defect -- but nobody has PROVEN the first arm is always the right one, and it is arm order deciding semantics.\n\n(2) REAL QUESTIONS THAT CANNOT SEPARATE (4 shapes: 00277_enum_tier0_fixture arms=3, behavior/enum_map_family arms=3, behavior/enum_predicate_search arms=3, behavior/fz_f98_range_map_converges arms=2). Example from 00277, one callsite, three arms on the SAME function: surfaces [:tail],[:tail],(...) / [int],[int],(...) / [{any,any}],[{any,any}],(...). All three project to the same RuntimeTypePredicate -- 'a list' -- because the predicate records list SHAPE (Empty/NonEmpty) and tuple ARITY only, never element types. Source order alone routes every list to whichever arm the scheduler happened to put first, and the three bodies are different specializations (different lanes/reprs).\n\nTHE DIRECTION (do not start without review): fz-kdt.104's cure -- drop the arm -- does not apply, because no arm's body covers the others. The cure has to be the other side: either the runtime predicate learns to answer the question the arms actually differ on (element-type/payload discrimination is NOT free at runtime -- it is O(n) on a list), or the analysis must not mint sibling specializations it cannot dispatch between (join them at the callsite instead, one body for one runtime question). Measure both before choosing. The FIFO/LIFO evidence for (2) is unmeasured: check whether any of the four flips behavior under Agenda::pop -\u003e pop_back before pricing a cure.",
  "id": "fz-kdt.107",
  "labels": [],
  "parent": "fz-kdt",
  "priority": 2,
  "status": "open",
  "title": "Runtime-indistinguishable dispatch arms where neither covers the other: the predicate must get stronger, not the plan smaller",
  "type": "bug",
  "updated_at": "2026-08-31T23:11:01Z"
}
