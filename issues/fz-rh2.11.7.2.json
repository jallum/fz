{
  "assignee": "",
  "blocked_by": [
    "fz-rh2.11.7.1"
  ],
  "blocks": [
    "fz-rh2.11.7.4",
    "fz-rh2.11.7.9"
  ],
  "created": "2026-06-09T15:52:08Z",
  "description": "Goal: bring compiler2's parser output in-house by building rooted Fz-shaped quoted source values directly, so `IndexCode` no longer stores shared `src/ast::Item` trees or a parallel compiler2 Rust AST as source authority.\n\nSignal:\n- `IndexCode` records `{heap, root}` for top-level code, where `root` names the parser-built quoted source value inside the compiler2 source heap\n- `ModuleSourceKind` and code/module source records identify Fz-shaped source roots, not shared `Rc\u003cItem\u003e` trees\n- parser tests cover Elixir-shaped source forms needed by the mini-epic: function/macro definitions, modules, attributes, alias/import/require, protocol forms, quote/unquote, item calls, pipe/headless case, capture syntax, maps/structs/bitstrings, and patterns\n- macro expansion can feed parser-built `{heap, root}` directly to the compile-time interpreter and receive macro-returned roots back relative to the same heap\n- legacy parser/AST remains legacy-owned; compiler2 does not ask the old frontend to pre-expand or desugar source\n\nChange:\n- add a compiler2 parser/front-door module that allocates/builds the quoted source values defined by `.11.7.1`\n- add first-class `require` syntax or equivalent source form support at this boundary\n- move compiler2 source discovery (`discover_modules`, protocol/module indexing) over Fz-shaped source readers/cursors\n- decide by inspection whether the existing lexer is neutral enough to reuse; fork lexer behavior too if tokenization embeds old AST policy\n\nWhy:\n- quoted source production cannot be correct-by-construction if the parser first commits to an old Rust AST shape\n- `require` and source-order macro availability are parser/source facts, not downstream lowering concerns",
  "id": "fz-rh2.11.7.2",
  "labels": [],
  "parent": "fz-rh2.11.7",
  "priority": 1,
  "status": "open",
  "title": "Compiler2 parser front door: build Fz-shaped quoted source directly",
  "type": "task",
  "updated_at": "2026-06-09T16:16:21Z"
}
