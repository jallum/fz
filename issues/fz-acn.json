{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-09-14T15:57:39Z",
  "description": "Proposal: ../interp-proposal.html (One Plan Fact). Measured 2026-09-14 on main f26264572.\n\nOne question, \"which inputs does this plan read, and how deeply\", is answered by four walks at four moments over two node sets: required_dispatch_input_ordinals (compiler2/artifact.rs, graph DFS, run by the interpreter on EVERY call on two paths), local_dispatch_mask (jobs/keying.rs, matrix arms including dead arms plus every guard, run per function per root), tested_input_ordinals (dispatch_reachability.rs, all nodes, any guard means every input), and four subject-to-root recursions under them. Two of the answers can disagree today (arms after a catch-all).\n\nThe interpreter walk is 19.6% of samples on tail_recursion (interp 3.90s vs run 0.16s); the dispatch executor's per-node HashMap clone and lane-vector clones are another quarter.\n\nDesign: DispatchGraph carries input_demand: Vec\u003cDispatchDemand\u003e, one slot per input ordinal, accrued in DispatchGraphBuilder::add_node as each node is created. Every Test and Outcome node is reachable, only the Fail node can be orphaned and it reads nothing, so accrual at creation equals the reachable walk without the walk. required(i) is input_demand[i] != Ignore; keying reads the same vector. The builder is handed the producer's view of pins and guards; guard leaves are accrued while guard_expr_from_ast builds the expression. Pins bind at creation (the post-build loop is deleted). The guard carrier subject (input 0) is never charged.\n\nProof: the fold lives inside the builder and nothing at runtime can call it; the census pins plans per fixture; one telemetry event per compiled plan carries the node counts DispatchCompileStats already computes and drops. Wall clock corroborates.\n\nChildren in DAG order: .1 and .2 (landed) feed .3 (the payoff); then the interpreter dispatch pair as a 1-2 punch: .4 (one door, one context struct with the walk as methods, one operand builder, arguments borrowed) and fz-21x.27 (dense state with an undo journal, lazy prepared keys, as representation changes inside that context; .5 folded into it). .6 (delete DispatchCallMiss) and .7 (Rc plan facts) are independent. .8 (zero-arity guard panic) closes by construction in .3 with its own fixture commit. .9 and .10 folded into .2. Follow-ups .11 (tested flavor for reachability, after .3) and .12 (one body table, after .4; the door then returns the target). Outside the epic: fz-oka (lambda size names a capture), fz-b58 (interpreter never spends reductions), fz-8ae (pinned map key panics), fz-tbm (multi-parameter fn does not parse), fz-mkb (size-name span).\n\nSweep PR (2026-09-15): stacked on #168 with gh stack (branch fz-acn/sweep, base fz-acn/one-plan-fact), one commit per ticket, in this order: fz-acn.12 (one body table; the door returns the target), fz-uai (input_demand.derived reaches the trace), fz-oka (a lambda's bitstring size names a capture), fz-kdg (one binary constructor, judged by guard_plan_inputs' allocation pin), fz-8ae (a pinned map key is a prepared value fed from the pin's word; the exhaustiveness analysis stops calling expect on user input). Rule for every commit: no golden or pin rises. Out of the sweep: fz-acn.11 (own PR, measured by the census), fz-ex9 (own tiny PR against main, user's decision), fz-tbm/fz-mkb (parser, unrelated), fz-r8x (native lowering, own PR), fz-b58, fz-6gc.",
  "id": "fz-acn",
  "labels": [],
  "priority": 1,
  "status": "open",
  "title": "[EPIC] One plan fact: a dispatch plan records what it reads of its inputs when its graph is built",
  "type": "epic",
  "updated_at": "2026-09-15T06:17:58Z"
}
