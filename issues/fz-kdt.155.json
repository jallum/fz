{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "closed_at": "2026-09-02T12:06:34Z",
  "created": "2026-09-02T10:47:53Z",
  "description": "Found by the fz-kdt.152 build (2026-09-02). PRE-EXISTING: identical at 62b5b09fe and after fz-kdt.152's fix, so it is a distinct root.\n\n  fn main() do\n    send(self(), fn (x) -\u003e dbg(x * 2) end)\n    f = receive do g -\u003e g end\n    Enum.each([1, 2, 3], f)\n  end\n\ninterp prints 2/4/6 (correct). JIT and AOT print the FIRST element only and\nthen abort:\n\n  fz_closure_get_capture_atom closure: ExpectedTag { expected: ValueKind(4), found: ValueKind(13) }\n  (runtime/src/ir_runtime.rs:1777) -- panic in a function that cannot unwind\n\nValueKind(4) is CLOSURE and ValueKind(13) is INT, so the native side reads a\ncapture slot expecting the closure it stored and finds the element instead:\na capture-lane/ABI mismatch on the each path specifically. Enum.map,\nEnum.filter and Enum.reduce with the same mailbox callable are correct on all\nthree doors after fz-kdt.152 (pinned by fixtures2/behavior/mailbox_closure_reduce.fz\nand mailbox_closure_enum_hofs.fz); each is the one HOF still red, and it is\nred only at the native door, which is why fz-kdt.152 could not pin it as a\ncompanion.\n\nThird three-path parity break in the closures x mailbox intersection\n(fz-kdt.130, fz-kdt.152). Suspect Enum.each's own wrapper closure -- each\ndiscards the reducer result, so its lambda's capture surface differs from\nmap's; compare against Prim::ClosureCapture's repr-from-the-storing-boundary\nrule (fz-kdt.125/.128).",
  "id": "fz-kdt.155",
  "labels": [],
  "parent": "fz-kdt",
  "comments": [
    {
      "text": "LANDED 910a023f2 (2026-09-02). A ROUND OF THREE: first fix BLOCKED by\nits refuter (the widen predicted one side of a two-sided fact -- a\ngrounded sibling callsite still pulled the member to zero and the\nmismatch MIRRORED: a_mixed green at base, abort at the first fix);\nrebuild landed the two-sided law with TWO evidence-forced corrections\nto the refuter's own spec (the seam gate is the callee value's\nCARRIER not the callsite's demand -- the builder's own tripwire caught\nadd1.(1);add1 under the spec'd gating; the bootstrap is\nNON-EMPTY-not-whole -- whole coarsened destination-passing across 29\nfixtures); re-refutation FIX-THEN-MERGE, three fixes folded (the\ntripwire's refusing half now has a unit witness -- neutering it had\nshipped green through every gate, the fz-kdt.157 pattern's second\ninstance; pipeline.md's over-strong 'demands every member whole' and\ndead Absent example corrected to the bottom-only bootstrap + the\n354/354 census + the residual family).\n\nTHE LAW: the boxed apply seam has ONE return convention, applied from\nboth sides -- producer: a seam member bootstraps to whole exactly at\nBOTTOM (shape axis only); consumer: a callsite whose callee VALUE\ntravels in the boxed carrier demands its result whole; grounded\ncallees alias the target's own fact; packaging tripwire\nverify_boxed_apply_seam_return_convention refuses any lane\ndisagreement (proven two-sided by reverting each half -- the original\nabort class is now a NAMED COMPILE ERROR forever).\n\nSCOREBOARD: fixes the original each abort + BOTH mixed-provenance\nwitnesses + h_cost (a base abort the refuter's own probe carried\nunnoticed) + five more pre-existing aborts (r1_selfapply,\nr8_two_arity1_wrappers, fp2, fp3, k_userstep); JIT alloc parity with\ninterp restored (the old 'free' path was free by miscompiling);\ni_cost_grounded 8→8 (never-boxed stays free); 2 pre-existing canon\nmovers (spawned server/0 wrappers → one raw atom lane, ZERO measured\nalloc cost, refuter-verified better than claimed); no new schedule\ndependence; convergence clean incl. the naive-variant's casualty.\nResidual family (zero-lane-callable seam member → wrapper stays\nAbsent → tripwire refuses, pre-existing aborts at base) = fz-kdt.159;\ninterp-only d1_call_and_return failure = fz-kdt.160.",
      "timestamp": "2026-09-02T14:04:41Z"
    }
  ],
  "priority": 1,
  "status": "closed",
  "title": "P1: a mailbox closure passed to Enum.each aborts on JIT and AOT",
  "type": "bug",
  "updated_at": "2026-09-02T14:04:41Z"
}
