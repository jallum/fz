{
  "assignee": "Jason Allum",
  "blocked_by": [],
  "blocks": [
    "fz-5xp.82"
  ],
  "closed_at": "2026-09-12T14:40:51Z",
  "close_reason": "completed",
  "created": "2026-09-07T22:05:29Z",
  "description": "## What is unreachable\n\n`Enumerable` declares `slice/1` and three implementations answer it:\n\n```\nlib/enumerable.fz:68   fn slice(_list), do: {:error, :unsupported}\nlib/enumerable.fz:76   fn slice(map), do: Map.slice(map)\nlib/enumerable.fz:85   fn slice(%Range{...}) do ... Range.slice(...) end\n```\n\nNothing calls it. There is no `Enum.slice` in `lib/enum.fz`, and no other caller of `Enumerable.slice` anywhere in `lib/`. `Map.slice/1` and `Range.slice/6` exist only to be reached through it, so they are unreachable too -- `Range.slice/6` is the reason `Range.reverse/2` exists, which is the private helper in fz-5xp.92.\n\nFor lists the callback would not help even if something did call it: `List`'s implementation returns `{:error, :unsupported}` unconditionally.\n\n## The choice\n\nEither `Enum.slice/2,3` is coming and this is scaffolding waiting for its consumer, or it is not and this is a protocol callback, three implementations and two helper functions carrying maintenance weight for nobody.\n\nIf it is the former, this ticket is a note that the scaffolding predates its consumer and should be finished. If the latter, it is a deletion: the callback, the three `defimpl` bodies, `Map.slice/1`, `Range.slice/6`, and `Range.reverse/2` all go together.\n\n## Found how\n\nDocumenting `Enumerable`'s callbacks: writing an honest `@doc` for `slice/1` meant naming the `Enum` function that reaches it, and there is none.",
  "id": "fz-5xp.93",
  "labels": [],
  "parent": "fz-5xp",
  "comments": [
    {
      "text": "Decision: finish the existing slicing scaffold. Enumerable.slice/1 is the protocol callback that should power public Enum.slice/2,3; the names do not conflict because they belong to different modules. This ticket will add the consumer and remove slice/2,3 from fz-5xp.82 rather than delete the optimized Map and Range paths.",
      "timestamp": "2026-09-12T13:57:48Z"
    },
    {
      "text": "Completed in 008a1e1f0. Added public Enum.slice/2 and slice/3 with direct list traversal, the existing unary Map slicing callback, the existing ternary Range slicing callback, and a generic Enumerable.to_list fallback. Negative indexes are normalized after counting; zero amounts and empty or out-of-bounds windows return an empty list; negative amounts and nonpositive range steps abort. The oracle fixture covers lists, maps, ranges, positive and negative bounds, empty input, out-of-bounds starts, zero amounts, and overshooting strides on all three execution doors. Evidence: 724/724 fixture legs; 2020 compiler library tests passed with 6 ignored; 260/260 runtime; 30/30 CLI; workspace doctests; AOT variadic-open; clippy with warnings denied; fmt; diff check. Review also produced fz-5xp.108 for incorrect runtime clause selection when a guard reads a destructured struct field. The known direct-only callable transport limitation remains owned by fz-kdt.117; these protocol callbacks are consumed in the receiver-specific clause rather than transported through a helper.",
      "timestamp": "2026-09-12T14:40:50Z"
    }
  ],
  "priority": 3,
  "status": "closed",
  "title": "Enumerable.slice/1 is surface nothing reaches",
  "type": "task",
  "updated_at": "2026-09-12T14:40:51Z"
}
