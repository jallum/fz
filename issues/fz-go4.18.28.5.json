{
  "assignee": "Jason Allum",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-07-02T22:11:01Z",
  "description": "Found while landing fz-go4.18.28.1 (measured at fefa3e0d2). The transport product path's WAIT EMISSION ORDER is load-bearing for plan completeness: with unordered waits (HashSet order) from the closure expansion (outgoing_input_edge_waits / incoming-slot waits / RuntimeDemand waits), compiler2_membership_operator_protocol_receivers_settle_to_direct_impls fails with 'transport plan is missing required call return-flow position ReturnPayload{...}' — the shape for a caller's ReturnPayload is only recorded when SOME component pull lands in an epoch whose graph still contains that position (the union SHRINKS as materialized bodies displace lowered bodies: observed 826-\u003e785 positions across epochs on that fixture), and the wait order steers which epoch each pull lands in. Canonical structural wait ordering (restored in fz-go4.18.28.1 via sorted_executable_waits) protects one schedule; the underlying contract — every plan-required position's shape must be recorded regardless of pull schedule — is not enforced by construction. Evidence: instrumented run log in fz-go4.18.28.1's session; repro = drop the sorted_executable_waits calls in src/compiler2/jobs/transport.rs and run that test. Direction: make the plan's required positions an explicit product-level demand (each required position waits until its shape fact is Shape or provably Absent-for-the-FINAL-epoch), or record return-payload shapes from the materialization seam itself instead of relying on epoch timing.\n\nBROADER EVIDENCE (fz-go4.18.24 first-family audit, 2026-07-02): scheduler nondeterminism is not confined to the transport plan. --log-telemetry job trace on fixture 00001_quicksort_plus_foo.fz, same binary, 5 consecutive reruns: total telemetry event count varied 3674-3705, job-count 470-484, applied-steps 238-245 -- output (stdout) was byte-identical across all runs, but the INTERNAL job trace was not, purely from scheduling/iteration-order nondeterminism (HashSet iteration order is the suspected source, consistent with this ticket's own root cause). This means empirical job-trace-count comparisons (before/after a change) are NOT a reliable audit signal on their own across this codebase until this ticket lands -- audits should prefer static producer/wait-graph proofs (as fz-go4.18.24's first family used) over raw telemetry event-count diffing, or force deterministic iteration (BTree on structural keys, per the sorting-is-a-barrier law) before trusting count-based before/after comparisons.\n\nORD-ON-INTERNED-IDS WATCH (from fz-go4.18.28.2's audits, 2026-07-02, at c007d2e2d): .28.2 derived Ord/PartialOrd on ValueId, FunctionId, RootId, ActivationKey, ExecutableNeed, ExecutableKey to key a BTreeMap in seed_callable_capture_inputs. ActivationKey's derived Ord compares its arrow Ty -- a raw interned id whose allocation order is process-hash-dependent -- so any BTreeMap/sort on these types gives PER-PROCESS determinism only, never cross-run stability (parity with the old sort key it replaced, audited: order feeds visitation only, anchor conflicts resolve by assert-agree, so no semantic exposure TODAY). Two standing risks this ticket should resolve or guard: (1) the derives make raw-interned-id ordering ambiently available on the compiler's core identity types -- any future site that sorts on them and lets the order decide anchoring/inclusion/first-wins re-creates this ticket's exact bug class silently; (2) when this ticket makes plan completeness wait-order-independent and deletes sorted_executable_waits, sweep for any interim code that started leaning on ActivationKey/ExecutableKey ordering for cross-run expectations. Consider a lint/comment on the derives once the load-bearing sort is gone.",
  "id": "fz-go4.18.28.5",
  "labels": [],
  "parent": "fz-go4.18.28",
  "priority": 2,
  "status": "in_progress",
  "title": "Transport plan completeness is pull-wait-order dependent (schedule-protected path dependence)",
  "type": "bug",
  "updated_at": "2026-07-03T00:46:20Z"
}
