{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-09-03T04:10:55Z",
  "description": "From the fz-kdt.120 research (2026-09-02, d2a5f3510). A free type variable\nintersects every concrete kind to `none`, so a union member that carries one\nis silently DELETED:\n  {:cont,{[int],int}} | {:halt,{[int],int}}  ∩  ({:cont,b} | {:halt,c})          = none\n  {:cont,{[int],int}} | {:halt,{[int],int}}  ∩  ({:cont,{[int],int}} | {:halt,c}) = {:cont,{[int],int}}   \u003c- :halt arm gone\n  (int,{[int],int}) -\u003e {:cont,..}|{:halt,..}  ∩  (a,b) -\u003e {:cont,b}|{:halt,c}     = (int | a0, a1 | {[int],int}) -\u003e none\njobs/semantic.rs refine_contract_inputs guards only the WHOLE-input empty case\n(if is_empty(refined) { input }), so the partial erasure passes as a\n\"refinement\", keys the callee's activation on the narrowed row, and the callee's\nentry_dispatch loses the arm. Measured consequence (prototype with facts-only\ncontract params): List.reduce_while_step/3 keyed [.., {:cont,{list,int}}, ..]\ninstead of [.., {:cont,{list,int}} | {:halt,{list,int}}, ..], its entry_dispatch\nlost the whole :halt arm (46 canon lines) and Enum.take([1,2,3,4,5], 3)\nprinted the whole list -- 15 behaviour movers, all early-halt (take, find,\nfind_index, count, reduce_while). contract.rs narrow_args_to_clause ALREADY\nskips var-carrying positions (:232); refine_contract_inputs does not: two\nsibling narrowing paths of one feature disagree. WARNING: adding the same skip\nto refine_contract_inputs alone at HEAD takes 00277_enum_tier0_fixture from\ngreen to rc=101 -- HEAD's correctness currently DEPENDS on narrowing closure\narguments by a var-carrying arrow surface. Direction: Types::intersect must\ntreat a free variable as `any` for the purpose of set operations (a variable\ndenotes an unknown SUBSET, and intersecting with an unknown cannot delete),\nor refuse the operation; then the guard becomes unnecessary and the 00277\ndependency must be understood (what does it rely on?). Blocks the principled\nfacts-only parameter policy for @spec instantiation (fz-kdt.120's residue).",
  "id": "fz-kdt.181",
  "labels": [],
  "parent": "fz-kdt",
  "comments": [
    {
      "text": "PARAMETER-SIDE REPRODUCER from the fz-kdt.210 research (2026-09-03, base dd07432db), possibly the same defect as yours or a close sibling -- your owner should decide.\n\nfz-kdt.210 fixes the RETURN side of a partial join: a @spec variable whose covariant occurrences include one the walk cannot read (an opaque reducer arrow handed over as a bare address var, so collect_arrow_match finds no callable_clauses and answers Unknown) was being treated as fully joined, so instantiate_match called the result Known -- a runtime fact -- from a join that never finished.\n\nThe PARAMETER half of that same root is a FALSE spec/violation, present at base AND under 210s prototype: refine_contract_inputs instantiates the reducers arrow from the same partial join and clamps it to the SEED accumulator, so the ascent stops one rung short. Reproducer is `.kdt210/probe/seed.fz` in the fz-kdt.210 research artifacts (a fold whose accumulator seed is an atom); swapping the seed to `[]` makes it compile, because the fz-f98.16 empty-list cleaner masks the list case -- which is why this has been invisible.\n\nThat is the same shape your body describes -- refine_contract_inputs consuming an erasure as a refinement -- with the erasure arriving from a partial instantiation rather than from intersect deleting a var-carrying union member. If they are one defect, fold; if two, this is the sibling and wants its own ticket. Related: fz-kdt.132 (closed -- the clamp this resembles, arriving from a @spec rather than a closure clause), fz-kdt.210.",
      "timestamp": "2026-09-03T22:37:08Z"
    }
  ],
  "priority": 1,
  "status": "open",
  "title": "Types::intersect deletes a union member whose surface member carries a free type variable, and refine_contract_inputs consumes the erasure as a refinement",
  "type": "task",
  "updated_at": "2026-09-03T22:37:08Z"
}
