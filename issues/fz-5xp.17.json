{
  "assignee": "",
  "blocked_by": [
    "fz-5xp.1",
    "fz-5xp.12",
    "fz-5xp.13",
    "fz-5xp.14",
    "fz-5xp.15",
    "fz-5xp.16",
    "fz-5xp.18",
    "fz-5xp.2"
  ],
  "blocks": [],
  "closed_at": "2026-09-07T16:08:39Z",
  "created": "2026-09-06T23:44:37Z",
  "description": "THE GOAL of fz-5xp. Everything else exists to make this writable.\n\nA complete JSON parser and serializer in fz, plus a small query driver, with a\nhand-written Elixir twin as the oracle and pinned allocation goldens. ~300-400 lines\nacross 4-6 modules, against a current ceiling of ~22 executable lines\n(behavior/quicksort.fz, the largest real program fz has ever run).\n\nShape:\n  input = \"{\\\"users\\\":[{\\\"name\\\":\\\"ada\\\",\\\"age\\\":36}]}\"\n  {:ok, doc} = Json.decode(input)\n  dbg(Json.encode(doc))\n  dbg(Enum.map(doc[\"users\"], fn u -\u003e u[\"name\"] end))\n  dbg(Process.heap_alloc_stats())\n\nWhy JSON specifically:\n  - self-contained. fz has NO argv, NO stdin, and no way for arbitrary libc calls to\n    return bytes -- but a runtime-provided extern CAN return a binary\n    (fz_binary_concat proves it). JSON takes an embedded literal and prints.\n  - forces the library by demand: scanning -\u003e String + native bitstring dispatch;\n    objects -\u003e a real Map; arrays -\u003e Enum builders; numbers -\u003e Integer/Float format;\n    errors -\u003e {:ok,_}|{:error,_}, which is the only error idiom fz has (no exceptions).\n  - measures BOTH costs. The parser is first-order recursion, where TRMC and owned-cons\n    reuse should shine. The query driver is HOF-heavy, where the indirect-call tax\n    shows: there is no inliner anywhere, the callable slot is blind at the recursive\n    core, and artifact.rs:1081 makes any position reached by two distinct lambdas a\n    boxed ValueRef -\u003e CallEdge::Indirect. One program, both signals.\n  - round-trip decode(encode(x)) == x is a second oracle independent of stdout.\n\nAcceptance: green on run + interp + build against ONE Elixir-owned golden, plus\nallocation goldens that are argued, not merely recorded.",
  "id": "fz-5xp.17",
  "labels": [],
  "parent": "fz-5xp",
  "comments": [
    {
      "text": "GOAL PROGRAM RUNS, landed at 71cab6c75.\n\n`Json.decode/1` and `Json.encode/1` in src/modules/runtime_library/json.fz,\noracled against Elixir's OWN `JSON` module -- 36 assertions across decode,\nencode and round-trip, byte-identical on interp, run and build, with\ndecode(encode(x)) == x for every value type.\n\nCovers: true/false/null, integers, negatives, fractional numbers, strings with\n\\\" \\\\ \\n \\t escapes, arrays, objects, arbitrary nesting, and an error result\nfor malformed input.\n\nNOT YET, so this stays open:\n  * exponent notation in JSON INPUT (1.0e-7). fz source can write it now\n    (fz-5xp.39) but the decoder's number grammar does not read it;\n  * unicode \\u escapes;\n  * rejecting trailing garbage inside containers -- the top level rejects it,\n    the containers are more permissive than the grammar;\n  * allocation goldens on a real workload, which the epic's SIGNAL asks for and\n    which fz-5xp.49's map sorting now makes worth measuring.\n\nEverything the plan predicted as a blocker turned out not to be, and everything\nthat actually blocked was found by writing the program: fz-5xp.52, .6, .48, .15,\nand Map.put's typed clause family. Recorded in the commit message.",
      "timestamp": "2026-09-07T11:25:42Z"
    },
    {
      "text": "ALLOCATION GOLDEN LANDED, which was the fourth and last gap.\n\nfixtures2/behavior/json_decode_cost.fz decodes the same document shape at two\nsizes -- 51 bytes and 135 -- and pins the RATIO rather than a number:\n\n    :shared_bin_bytes   0 and 0\n    :bitstring_allocs   312 and 622   (interp)\n                        489 and 1099  (run/build)\n\n2.6x the document, about 2x the allocations, nothing copied.\n\nRecording a single number would have been worse than nothing. The first attempt\nat this golden would have pinned 435 allocations for a 68-byte document as\ncorrect; what that number actually described was a quadratic scan, now fixed as\nfz-5xp.55. A ratio is the smallest thing that can be argued with.\n\nStatus of the four gaps recorded when the goal program landed:\n  * exponent notation in JSON input      -- DONE (99537f6d8)\n  * unicode \\u escapes                   -- DONE (99537f6d8)\n  * trailing garbage inside containers   -- DONE (99537f6d8)\n  * allocation goldens on a real workload -- DONE (this)\n\nThe ticket stays open only on its formal blockers (.13 Keyword, .14 String,\n.15 to-string's two unmet requirements, .16 Enum, .8 term ordering), which are\nlibrary completeness rather than anything the goal program needs to run. It has\nrun on all three doors since 71cab6c75.",
      "timestamp": "2026-09-07T13:50:08Z"
    },
    {
      "text": "THE GOAL IS MET, and every blocker is closed.\n\nThe ticket's own example, verified on interp and run:\n\n    input = \"{\\\"users\\\":[{\\\"name\\\":\\\"ada\\\",\\\"age\\\":36}]}\"\n    {:ok, doc} = Json.decode(input)\n    Json.encode(doc)                                  -\u003e the document back\n    Enum.map(doc[\"users\"], fn u -\u003e u[\"name\"] end)     -\u003e [\"ada\"]\n    Enum.join(..., \", \")                              -\u003e \"ada\"\n    String.upcase(...)                                -\u003e \"ADA\"\n\nThe last two lines are the point of the epic rather than decoration: Json, Enum\nand String are three modules written in fz, composing, over a value the parser\nbuilt -- and String and half of that Enum surface did not exist when the goal\nprogram first ran.\n\nACCEPTANCE, as the ticket stated it:\n  * green on run + interp + build against ONE Elixir-owned golden --\n    json_roundtrip, 55 assertions oracled against Elixir's own JSON module;\n  * allocation goldens that are ARGUED, not merely recorded -- json_decode_cost\n    pins a RATIO across two document sizes rather than a number, because the\n    number that would have been pinned first described a quadratic scan\n    (fz-5xp.55).\n\nAll four gaps recorded when the program first ran are closed: exponents,\nunicode escapes, trailing garbage in containers, and the allocation golden.",
      "timestamp": "2026-09-07T16:08:39Z"
    },
    {
      "text": "API ALIGNED to Elixir's, read from lib/elixir/lib/json.ex (223a310aa). The goal\nprogram matched on VALUES but had invented its own surface.\n\n    Elixir                       fz before          fz now\n    decode/1                     decode/1           decode/1\n    decode!/1                    --                 decode!/1\n    encode!/1,2                  encode/1           encode!/1\n    encode_to_iodata!/1,2        --                 -- (fz has no iodata)\n    protocol_encode/2            --                 -- (no JSON.Encoder protocol)\n\nTHERE IS NO PLAIN encode/1 in Elixir: encoding can only fail for values the\nprotocol does not accept, so the only spelling is encode!/1.\n\nThe decode ERROR is Elixir's positioned reason rather than the leftover input:\n\n    {:unexpected_end, offset} | {:invalid_byte, offset, byte}\n    | {:unexpected_sequence, offset, sequence}\n\nInternally a failure still carries the rest -- what a recursive-descent parser\nhas in hand -- and the offset is recovered once at the boundary, so nothing is\nthreaded through the parser.\n\nThree rules found by RUNNING Elixir rather than reasoning about it, all pinned\nin the fixture: a keyword that starts and does not finish reports unexpected_end\nat the end of input (even 'truX'); trailing junk reports the position of the\nWHITESPACE before it; and a bad \\uXXXX is unexpected_sequence at the backslash\nwhile a bad simple escape is an invalid_byte at the letter.\n\nThe fixture's seven malformed cases asserted only ok?/1 and threw the reason\naway; they now assert the whole reason.\n\nNOT FOLLOWED, and each for a reason: encode_to_iodata!/1,2 needs iodata;\nprotocol_encode/2 and the JSON.Encoder protocol need a derivable protocol with\n:only/:except; decode/3's custom decoders need the acc/decoders design. Filed as\nfz-5xp.73.",
      "timestamp": "2026-09-07T18:44:44Z"
    }
  ],
  "priority": 1,
  "status": "closed",
  "title": "Json.decode/1 and Json.encode/1 -- the goal program",
  "type": "task",
  "updated_at": "2026-09-07T18:44:44Z"
}
