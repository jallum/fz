{
  "assignee": "",
  "blocked_by": [
    "fz-5xp.57"
  ],
  "blocks": [
    "fz-5xp.55"
  ],
  "closed_at": "2026-09-07T13:15:20Z",
  "created": "2026-09-07T12:49:02Z",
  "description": "TAG_MASK is FOUR bits and TAG_FWD is 0x8. A ProcBin stub keeps its SharedBin's\naddress in word 0, and word 0 is also where Cheney writes its forwarding\nmarker. A SharedBin was a 40-byte Box, so 8-aligned -- and roughly half of all\nsuch addresses end in 8. For those, `is_forwarded_procbin` reads a LIVE stub as\nforwarded:\n\n    to_p     = shared_ptr \u0026 !0xF          // the SharedBin, rounded DOWN by 8\n    new_head = to_p | TAG_PROCBIN         // head now points 8 bytes below it\n    ProcBin::from_raw(to_p).mso_next_set(new_head)   // writes INTO the SharedBin\n\nThe chain head becomes a pointer into the middle of an unrelated object and the\nrelease edge is lost.\n\nHOW IT SURFACED: matching more than one binary literal over 64 bytes printed\nevery answer correctly and then died at teardown with SIGBUS on `run`, silently.\n\n    one big literal, matched         -\u003e  exit 0\n    TWO big literals, both matched   -\u003e  exit 138\n\nThe two compiler-baked static SharedBins were emitted back to back at 0x...000\nand 0x...028: the first is 16-aligned and safe, the SECOND ends in 8. Sweeping a\nstub that pointed at it gave a head of first_sb + 32 -- the first one's\nDESTRUCTOR field -- so the teardown dump showed `sb == shared_bin_destructor_noop`\nand the refcount fetch_sub faulted writing into executable memory\n(EXC_BAD_ACCESS code=2). 'Two literals' was never the cause, only the way to\nland on the bad alignment reliably; heap-allocated SharedBins were a coin flip\non every run.\n\nFIXED (8201bdce8) by the invariant the heap already keeps: 16-byte alignment, so\nthe low four bits are never part of an address. SharedBin and Resource were the\ntwo things a heap stub points at that were left out of it, and the static copies\nwere emitted at align 8 to match. All three now say 16.\n\nInvisible until fz-5xp.57, because a binary that large could not be matched at\nall.",
  "id": "fz-5xp.60",
  "labels": [],
  "parent": "fz-5xp",
  "comments": [
    {
      "text": "ROOT CAUSE FOUND, and it is not about static binaries at all -- they were only\nthe first thing to trip it.\n\n    pub const TAG_MASK: u64 = 0b1111;   // FOUR bits\n    pub const TAG_FWD:  u64 = 0x8;\n\n    fn is_forwarded_procbin(addr) -\u003e Option\u003c*const u8\u003e {\n        let marker = read(addr);\n        if marker \u0026 TAG_MASK == TAG_FWD \u0026\u0026 marker \u0026 !TAG_MASK != 0 { Some(..) }\n    }\n\nWord 0 of a live ProcBin is its `shared_ptr`. A SharedBin is a 40-byte Box, so\nit is 8-aligned -- and ROUGHLY HALF OF ALL SharedBin ADDRESSES END IN 0x8. For\nthose, a perfectly live ProcBin reads as FORWARDED.\n\nWhat then happens in mso_sweep:\n\n    to_p     = shared_ptr \u0026 !0xF          // the SharedBin, rounded DOWN by 8\n    new_head = to_p | TAG_PROCBIN         // chain head is now 8 bytes BEFORE a SharedBin\n    ProcBin::from_raw(to_p).mso_next_set(new_head)   // writes into the SharedBin\n\n-- so the chain head becomes a pointer into the middle of an unrelated object,\nthe SharedBin gets a word overwritten, and the release edge is lost.\n\nTHE OBSERVED CRASH MATCHES EXACTLY. The two static SharedBins were emitted\nadjacent, 40 bytes apart, at 0x...000 and 0x...028. The first is 16-aligned\n(nibble 0, safe); the SECOND ends in 8. Sweeping a stub that pointed at it gave\nhead = 0x...020 = first_sb + 32 = the first SharedBin's DESTRUCTOR field -- which\nis why the teardown dump showed `sb == shared_bin_destructor_noop` and why the\nrefcount fetch_sub faulted writing into executable memory.\n\nIt also explains the trigger precisely: ONE static binary is 16-aligned and\nsafe; TWO puts the second at +40, ending in 8. Nothing to do with literals as\nsuch, and heap-allocated SharedBins are a coin flip on every run.\n\nTHE FIX, and it is an invariant the codebase already relies on everywhere else:\nheap objects are 16-ALIGNED so the low four bits are free for tags. SharedBin and\nResource are the two things a heap stub points at that were left out of it. Give\nboth `#[repr(C, align(16))]` and give the compiler-baked static `set_align(16)`,\nand a real off-heap pointer can never again be read as a tag.\n\nis_forwarded_resource is the same predicate on a Resource pointer and has the\nsame hole.",
      "timestamp": "2026-09-07T12:52:07Z"
    }
  ],
  "priority": 1,
  "status": "closed",
  "title": "A live ProcBin whose SharedBin address ends in 8 reads as forwarded, so the GC writes into it",
  "type": "bug",
  "updated_at": "2026-09-07T14:20:38Z"
}
