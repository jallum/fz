{
  "assignee": "Jason Allum",
  "blocked_by": [
    "fz-jg5.7"
  ],
  "blocks": [],
  "created": "2026-05-18T02:56:45Z",
  "description": "# Make declared @spec narrow downstream callers and gate reduction\n\n## Goal\n\n`fz-ul4.31` shipped @spec parsing + validation\n(`spec_check::validate_specs` enforces `inferred ⊆ declared`).\nWhat's missing:\n\n1. **Downstream callers see declared return.** When the typer types\n   a callsite `g(x)` and `g` has a declared `@spec`, the inferred\n   return Descr at that callsite is the declared return — not a\n   walk of `g`'s body.\n2. **Reducer treats @spec'd fns as boundaries by default.** The\n   user opted into emitting `g` as a body by declaring its spec;\n   downstream callers see the narrow declared return without\n   triggering reduction into `g`.\n\n## What this buys\n\n- A library author writes `@spec` once. Downstream callers reason\n  about the function via the spec, not the body. Mirrors the\n  user-facing contract: annotations are firewalls.\n- Compile time: reducer doesn't recurse into `@spec`'d bodies.\n- Code shape: `@spec`'d fns get one body each (the declared shape),\n  not per-callsite specializations.\n\n## Acceptance\n\n- A fixture demonstrating: a function `f` with `@spec :: int → int`,\n  called from main with `f(42)` and `f(some_runtime_int)`. After\n  this ticket, `f` has 1 body; both callers see return type `int`.\n- `fixtures/spec_ok` passes unchanged (it already declares `@spec`).\n- `spec_check::validate_specs` still passes for every fixture (the\n  declared bound is still a bound).\n\n## Removals\n\nNone directly. Reducer logic gains an early-stop branch for @spec'd\ncallees.\n\n## Future\n\n- Typed receive (`fz-ul4.19`) extends this story: a typed mailbox\n  is a boundary annotation with the same shape.\n- Typed externs already declare return Descrs; reducer can already\n  use them.",
  "id": "fz-jg5.12",
  "labels": [],
  "parent": "fz-jg5",
  "comments": [
    {
      "text": "Findings (docs/walkthroughs/FINDINGS.md): Ratify the 'trivially inlinable' criterion: an @spec'd fn is trivially inlinable iff its body is one block with one non-control statement plus Return. add1 qualifies (BinOp + Return). Multi-stmt / branching / recursive fns are firewalls. Per-callsite override (@inline :always / :never) is a follow-up not blocking the contract.",
      "timestamp": "2026-05-18T03:17:57Z"
    },
    {
      "text": "Wiring sketch for future session:\n\n1. Module gains a boundary field: `pub boundary_fns: HashSet\u003cFnId\u003e`.\n2. ir_lower::lower_program populates boundary_fns from Attr::Spec items\n   in the AST (a fn with a declared @spec joins the set).\n3. ir_reducer::try_reduce_call early-returns None when the callee is in\n   module.boundary_fns UNLESS trivially-inlinable (single block, one\n   non-control stmt + Return per FINDINGS.md ratification).\n4. Typer change (separate but related): callsite return Descrs to\n   @spec'd fns use the declared return rather than walking the body.\n\nAcceptance: a fixture where f has @spec :: int → int — main calls f\nwith both literal and runtime int args; after RED.9 f has exactly 1\nemitted body and both callers see declared return type.\n\nEmpirical risk to verify: removing the closure_lit branch in walk_spec_for_discovery (attempted in fz-jg5.9 session) broke closure_typed_captures/curried_add runtime. RED.9's boundary-marker may interact with that path — verify per-fixture before landing.",
      "timestamp": "2026-05-18T04:02:10Z"
    }
  ],
  "priority": 2,
  "status": "in_progress",
  "title": "fz-RED.9: @spec as downstream-narrowing contract",
  "type": "task",
  "updated_at": "2026-05-18T10:17:42Z"
}
