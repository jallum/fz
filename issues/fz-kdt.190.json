{
  "assignee": "",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-09-03T05:49:55Z",
  "description": "From the fz-kdt.183 research (2026-09-03, d2a5f3510), proven at HEAD with no\nprototype: two independent Enum.with_index statements; swapping their SOURCE\nORDER flips List.reduce_step/3's published ReturnType fact between\n{:done, [binary]} and {:done, [int]} with an identical key and identical\nrow set. Cause: jobs/semantic.rs analyze_activation (:222-229) refines the\nkey-wide joined return by EACH ROW's @spec result in row order, and\nrefine_observed_return (:1884, :1903-1905) returns the observed type\nunchanged when the intersection is empty -- so the first row's contract\nnarrows the join and every later row's contract is silently discarded.\nfz-kdt.183's element-keyed fix removes THIS instance (the two rows become two\nactivations) but the mechanism survives wherever a key still shares rows\n(the callable slot). Fix direction: a contract result may refine an\nactivation's return only when it is a fact about EVERY row the activation\ncarries (the meet over rows of each row's instantiated result, or nothing);\nand an empty intersection is a diagnostic about the row, never a silent\nno-op. Red-first: the swapped-order pair publishes the same fact.",
  "id": "fz-kdt.190",
  "labels": [],
  "parent": "fz-kdt",
  "comments": [
    {
      "text": "CONCORDANT with fz-kdt.65 (return-widening ladder resets only on rebase; join-only ascent) -- this is its join-only side made order-dependent by contract refinement. Also with fz-kdt.33 (spec enforcement waits for correlation-sound evidence). fz-kdt.183 removes the instance on List.reduce_step/3; the mechanism survives on any shared key.",
      "timestamp": "2026-09-03T08:19:08Z"
    },
    {
      "text": "fz-kdt.210 (filed 2026-09-03) is a SECOND, distinct defect at your seam. You own refine_observed_return keeping the observed on an empty intersection and the row-order dependence of the key-wide join. fz-kdt.210 owns refine_call_return MEETING a contract return that was instantiated from a GROUND seed argument: reduce_cont keyed [list(int), empty_list(), a2] publishes {:done, []} because `b|c := []` binds from the seed and `[] \u003c: list(int)` makes the meet []. Same function, different failure. Linking so neither is fixed in a way that re-breaks the other.",
      "timestamp": "2026-09-03T18:17:44Z"
    },
    {
      "text": "fz-kdt.210 landed (5a1ba823b): neither re-broken nor subsumed, measured. Your stated instance no longer reproduces at this head (fz-kdt.183 element keying) with or without 210 -- the swapped-order probe pair is identical on both binaries. Your half (a), row-order-dependent narrowing, gets strictly BETTER: the CARVE census (a constrained observed return narrowed to a strict subset) goes 8 -\u003e 0. Your half (b), an empty intersection kept silently, SURVIVES: CARVE_EMPTY_KEEP 13 -\u003e 9, the remainder all in 00144. So what is left of this ticket is half (b) plus a fresh reproducer, since the original one is gone.",
      "timestamp": "2026-09-04T00:02:27Z"
    }
  ],
  "priority": 2,
  "status": "open",
  "title": "A published return fact is source-order dependent: analyze_activation refines the key-wide join by each row @spec result in row order and refine_observed_return keeps the observed on an empty intersection, so the first row wins",
  "type": "task",
  "updated_at": "2026-09-04T00:02:27Z"
}
