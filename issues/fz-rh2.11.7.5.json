{
  "assignee": "",
  "blocked_by": [
    "fz-rh2.11.7.3",
    "fz-rh2.11.7.4"
  ],
  "blocks": [
    "fz-rh2.11.7.6"
  ],
  "created": "2026-06-09T15:52:40Z",
  "description": "Goal: execute compiler2 macro functions as compile-time Fz code over the same Fz-shaped quoted AST values produced by the parser and consumed by source production, without the old `CompileTimeEvaluator`, `exec::Value` AST codec, or repeated AST format conversion.\n\nSignal:\n- a local `defmacro inc(x) do quote do: unquote(x) + 1 end end` can run during source production and return a Fz-shaped quoted AST value\n- macro functions receive `__CALLER__` plus quoted Fz-value args; args are not evaluated and are not decoded to Rust AST first\n- macro return values are already quoted Fz-shaped AST; recursive expansion can consume them directly\n- quote/unquote allocate and splice process-heap source values, preserving lexical context metadata\n- macro execution errors surface through compiler2 diagnostics/telemetry with call-site and definition context\n- no fz2 path calls `frontend::macros::expand_program_with_types`, `exec::ast_value`, or the scratch `CompileTimeEvaluator`\n\nChange:\n- add compile-time process/interpreter entry points for macro functions defined by compiler2 services\n- define the macro ABI as `(__CALLER__, quoted_arg_1, ..., quoted_arg_N) -\u003e quoted_ast_value`\n- implement quote/unquote over the compiler2 Fz-shaped quoted source value model\n- validate/root returned AST values before recursive expansion continues, without decoding them through a Rust AST representation\n- define deterministic macro execution limits/signals for recursion/runaway expansion\n- add telemetry for macro execution start/stop/error carrying raw ids/source facts, not formatted strings\n\nWhy:\n- the old evaluator proves the semantics, but its value model and whole-program preload are the wrong substrate\n- macros are ordinary compiled compile-time functions over Fz data; their only authority is returned source data and compiler-service calls",
  "id": "fz-rh2.11.7.5",
  "labels": [],
  "parent": "fz-rh2.11.7",
  "priority": 1,
  "status": "open",
  "title": "Compiler2 macro runtime: execute defmacro functions on a compile-time process heap",
  "type": "task",
  "updated_at": "2026-06-09T16:04:22Z"
}
