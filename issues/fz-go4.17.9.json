{
  "assignee": "",
  "blocked_by": [
    "fz-go4.17.8"
  ],
  "blocks": [
    "fz-go4.17.10"
  ],
  "created": "2026-06-28T15:52:30Z",
  "description": "Goal\nMove native and dump front doors onto the same product-backed demand boundary as interp and macro readiness. Native must not wait on BackendProgram(root) by scheduling BuildBackendProduct(root), and dumps must not revive semantic/root artifact stages except where explicitly requested as legacy diagnostics.\n\nSignal\nLowerNativeProgram no longer returns follow_up [BuildBackendProduct(root)] for BackendProgram(root). Native/AOT obtains or drives RootBackendProduct through the product driver. DumpStage::Backend is product-backed. DumpStage::Semantic is either removed, renamed legacy, or documented as intentionally requesting the old semantic diagnostic path. 00181 no-dump and targeted dump telemetry do not show BuildBackendProduct self-scheduling.\n\nStrategy\nAdd a shared front-door helper for driving RootBackendProduct facts/products, then route native and backend dumps through it. Keep legacy semantic dump behavior only if tests and docs name it as legacy.\n\nRemoval steps\n- Remove the JobEffects::wait_on_settled(..., [Job::BuildBackendProduct(root)]) path from lower_native_program.\n- Remove dump routing that maps Backend dump to a visible BuildBackendProduct job if the product driver can serve it directly.\n- Delete stale tests that require BuildBackendProduct to appear as the readiness job for native/backend/macro paths.\n- If Semantic dump remains, rename or document it as legacy diagnostic and ensure it is not used by artifact/backend tests.\n\nVerification\nNative/backend dump focused tests; macro executable test; port macro tests; 00181 --log-telemetry post-processing; cargo clippy --all-targets -- -D warnings.",
  "id": "fz-go4.17.9",
  "labels": [],
  "parent": "fz-go4.17",
  "priority": 1,
  "status": "open",
  "title": "Pull front doors: remove native and dump push handoffs",
  "type": "bug",
  "updated_at": "2026-06-28T15:53:40Z"
}
