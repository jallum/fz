{
  "assignee": "Jason Allum",
  "blocked_by": [
    "fz-hwn.12",
    "fz-hwn.5"
  ],
  "blocks": [
    "fz-hwn.7"
  ],
  "created": "2026-06-13T21:24:40Z",
  "description": "Goal\nFinish the native/codegen half of the shared runtime transport model so compiler2 consumes settled callable and delivered-lane facts end to end, without re-deriving closure obligations from syntax, types, or semantic values.\n\nProblem being fixed\nfz-hwn.10, fz-hwn.11, and fz-hwn.12 proved the upstream model is now mostly correct: demand/layout already decide whether a value is omitted, lane-shaped, direct-only callable, or first-class callable. The remaining red is where native/codegen still re-litigate those decisions.\n\nVerified remaining model holes\n1. Native step lowering still lacks the omitted / never-materialize callable state.\n- In fixtures like `unused_callable_constructor_frontier`, upstream facts already say the callable input is omitted: it is never called, never escapes, and no callable boundary should exist.\n- But `src/compiler2/jobs/native.rs` still treats `BackendStep::FunctionRef` / `BackendStep::Lambda` as only:\n  - `DirectOnly` =\u003e keep structural\n  - otherwise =\u003e materialize now\n- That is wrong. Absence from `ExecutableRuntimeDemand.callable_materializations` is itself a settled fact: no runtime callable object should exist.\n- Repro/proof: `cargo test compiler2_abi_ready_does_not_publish_unused_callable_constructors --lib`\n\n2. Native codegen still lacks an explicit zero-lane delivered shape.\n- Upstream/native now legitimately settle some returns and delivered resumes to zero lanes when the result is ignored.\n- `src/compiler2/native_codegen/demand.rs` still models delivered shape as only `Value` or `TupleFields`, so zero lanes get smuggled through as an empty tuple-field shape.\n- `src/compiler2/native_codegen/terminator.rs` and nearby codegen logic then re-interpret that shape and still assume every non-tuple native return must deliver one value lane.\n- Repro/proof: `cargo test compiler2_native_program_jit_runs_spawn_then_receive_through_compiler2_codegen --lib`\n\n3. Some remaining reds are stale expectation tests, not compiler bugs.\n- Example: tests that still require `CallClosure` in native IR even though exact direct-only closures now lower as direct `Call` / `TailCall`.\n- These tests must assert the settled transport contract instead of the old closure-object path.\n- Repro/proof: `cargo test compiler2_native_program_routes_nontail_if_join_flow_through_continuation_entries --lib`\n\nOut of scope / separate bug\n- The selective-receive artifact panic around original lowered entry ids is a separate artifact seam, not part of this native/codegen transport ticket.\n\nDesign rule\n- Semantic facts determine meaning.\n- Settled runtime demand/layout determine representation.\n- Native/codegen consume that representation contract.\n- Downstream phases must not ask again whether an omitted value carries lanes or whether a callable should become a first-class object.\n\nScope\n- Consume settled runtime input, return, resume, capture, and callable-omission layouts in `src/compiler2/jobs/native.rs` end to end.\n- Add an explicit zero-lane delivered-shape case in `src/compiler2/native_codegen/*` and thread it through continuation shaping, return emission, halt/adapter selection, and boundary adaptation.\n- Keep native local values structural so direct-only callables remain direct until a true first-class boundary demands materialization.\n- Keep callable values completely unmaterialized when all downstream layouts are omitted.\n- Publish callable boundaries only when native actually materializes a first-class callable object.\n- Update stale tests that still assert old closure-object or mandatory delivered-lane behavior.\n\nCode seams\n- `src/compiler2/jobs/native.rs`\n- `src/compiler2/native_codegen/demand.rs`\n- `src/compiler2/native_codegen/terminator.rs`\n- `src/compiler2/native_codegen/driver.rs`\n- `src/compiler2/native_codegen/prim.rs`\n- nearby native/codegen tests in `src/compiler2/drive_test.rs`\n\nRemoval target\n- Delete remaining native helpers that recover callable obligations from lambda/function-ref syntax instead of settled layout/materialization facts.\n- Delete codegen assumptions that every native return is either one value lane or tuple-field fast path.\n- Delete stale tests that encode the old closure-object path when the settled transport model now proves a direct path.\n\nChecks\n- `cargo test compiler2_abi_ready_does_not_publish_unused_callable_constructors --lib`\n- `cargo test compiler2_native_program_jit_runs_spawn_then_receive_through_compiler2_codegen --lib`\n- `cargo test compiler2_native_program_routes_nontail_if_join_flow_through_continuation_entries --lib`\n- `cargo test --lib compiler2_native_program_`\n- `cargo test --lib compiler2_abi_ready_`\n- `cargo test --lib compiler2_interp_runs_`\n- No native path emits `MakeClosure` / `MakeFnRef` for values whose downstream layouts are all omitted.\n- No native/codegen path re-asks whether an ignored return carries a value lane after layout has already settled to zero lanes.\n- Native JIT/AOT tests pass for spawn/receive, direct lambda call, escaped lambda, opaque callable, enum reducer cases, quicksort, and direct closure-target fixtures.\n- Stale expectation tests are rewritten to assert the settled transport contract.",
  "id": "fz-hwn.6",
  "labels": [],
  "parent": "fz-hwn",
  "comments": [
    {
      "text": "Current seam after mirroring demand-shaped local values into native executable inputs and entry captures: native direct-only callable transport still collapses at DeliveredResume because ReturnAbi can only express Value or TupleFields. Repro: compiler2_native_program_jit_runs_enum_reduce_through_compiler2_codegen fails in LowerNativeProgram with 'native call arg 7 for callee 66 expected direct-callable 65 with 0 capture(s), observed Some(Runtime(Var(7)))'. This indicates a direct-only callable returning through a continuation resume boundary and being rebound as a runtime value. Next correct step is to generalize internal return/resume transport from ReturnAbi::{Value,TupleFields} to the same value-layout model used for runtime params/captures, then thread that through artifact planning, backend interpreter DeliveredResume binding, native entry signatures, delivered_args_for_abi, and NativeDemandAbi/codegen delivered-shape handling. Do not restore old closure materialization to satisfy the test.",
      "timestamp": "2026-06-14T01:44:18Z"
    }
  ],
  "priority": 1,
  "status": "open",
  "title": "Compiler2 native: lower direct callable demand without first-class closure materialization",
  "type": "task",
  "updated_at": "2026-06-14T19:15:22Z"
}
