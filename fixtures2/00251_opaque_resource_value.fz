defmodule R do
  @type t :: opaque resource(integer)

  def get_value(h), do: h.value
end

extern "C" def _resource_test_dtor(integer) :: nil
def dwrap(x), do: _resource_test_dtor(x)

def main() do
  r = make_resource(99, &dwrap/1)
  assert(R.get_value(r) == 99)
end
