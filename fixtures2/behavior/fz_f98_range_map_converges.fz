#---
# purpose: "fz-f98: Range and Map reduce users share a convergent bridge fixed point"
#---
#
# Regression target for fz-bfh.4. The semantic drive and transport plan now settle
# (fz-f98.3 fixed the closure capture-layout fragmentation); native lowering still
# materializes more than one native callable boundary for the single published
# bridge-reducer boundary (fz-f98.6).
fn main() do
  dbg(Enum.to_list(1..7//2))
  dbg(Enum.to_list(%{1 => :a}))
end
