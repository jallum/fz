#---
# purpose: "fz-f98: Enum.reduce/2 over Range materializes one coherent callable boundary"
# defer.build: fz-f98.7 reduce/2 :first|{:acc} accumulator does not thread back through the Range bridge (Enum.EmptyError); fz-f98.6 reducer boundary fragments into multiple native boundaries
# defer.interp: fz-f98.7 reduce/2 :first|{:acc} accumulator does not thread back through the Range bridge (Enum.EmptyError); fz-f98.6 reducer boundary fragments into multiple native boundaries
# defer.run: fz-f98.7 reduce/2 :first|{:acc} accumulator does not thread back through the Range bridge (Enum.EmptyError); fz-f98.6 reducer boundary fragments into multiple native boundaries
#---
#
# Regression target for fz-bfh.2. A multi-surface callable value must be
# resolved by producer identity/publication, not by native pick-first behavior.
fn main() do
  dbg(Enum.reduce(1..7//2, fn (x, acc) -> x * acc end))
end
