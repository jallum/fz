#---
# purpose: "named struct overloads reject plain maps with the same fields"
#---
defmodule Foo do
  defstruct [:value]
  @type t :: %Foo{value: integer}
end

def pick(x :: Foo.t), do: 1
def pick(x), do: 2

def main() do
  dbg(pick(%Foo{value: 7}))
  dbg(pick(%{value: 7}))
end
