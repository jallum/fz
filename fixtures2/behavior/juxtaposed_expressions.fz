#---
# purpose: "two expressions without a newline between them are rejected instead of silently discarding the second"
# expect: diagnostic
# diagnostic.code: parse/expected-token
#---
def main() do
  a = 1 2
  dbg(a)
end
