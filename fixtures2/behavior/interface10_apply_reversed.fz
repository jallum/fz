#---
# purpose: "reversing compatible callback admission preserves callback selection"
#---
def apply_one(f, x), do: f.(x)
def main() do
  dbg(apply_one(fn x -> x * 2 end, 2))
  dbg(apply_one(fn x -> x + 1 end, 2))
end
