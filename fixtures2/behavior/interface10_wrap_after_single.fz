#---
# purpose: "one callback provides the work control for the following tuple operation"
#---
def increment(x), do: x + 1
def double(x), do: x * 2
def wrap_after(f, x), do: {f.(x)}
def main() do
  dbg(wrap_after(&increment/1, 2))
end
