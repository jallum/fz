fn relay(), do: send(1, receive do x -> x + 1 end)

fn main() do
  spawn(relay)
  send(2, 41)
  got = receive do x -> x end
  assert(got == 42, "spawned child blocks on receive, then relays 41+1")
end
