# Targets

Target versions of agent docs live here only while a subsystem is still being
built toward that shape. When the code catches up, the target moves into
`.agent/docs/` and becomes current documentation.

There are no active target docs in this directory right now. Read
`.agent/docs/` for the current subsystem models.
